---
phase: 02-release-a-safe-web-commerce
plan: 04
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - gammamarkets/__init__.py
  - gammamarkets/views.py
  - gammamarkets/templates/gammamarkets/admin.html
  - gammamarkets/templates/gammamarkets/public_product.html
  - gammamarkets/templates/gammamarkets/public_order.html
  - gammamarkets/static/gammamarkets/js/admin_app.js
  - gammamarkets/static/gammamarkets/js/admin_orders.js
  - gammamarkets/static/gammamarkets/js/admin_catalog.js
  - gammamarkets/static/gammamarkets/js/admin_publications.js
  - gammamarkets/static/gammamarkets/js/admin_settings.js
  - gammamarkets/static/gammamarkets/js/admin_notifications.js
  - gammamarkets/static/gammamarkets/js/public_checkout.js
  - gammamarkets/static/gammamarkets/js/public_order.js
  - gammamarkets/static/gammamarkets/js/public_storefront.js
  - tests/runtime/test_admin_ui.py
  - tests/runtime/test_buyer_ui.py
  - tests/runtime/test_release_a_journey.py
autonomous: true
requirements:
  - PUB-02
  - ORD-01
  - NOTF-01
  - UI-01
  - UI-02
  - UI-03
  - WEB-01
  - WEB-02

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "The admin shell mounts inside `base.html` with one nav section (Orders / Catalog / Publications / Settings) and every mutating control carries a text label — no icon-only mutators anywhere (02-UI-SPEC)."
    - "A2 checkout renders the merchant's Editorial/Guided/Compact preset with compact forced ≤560px, the invariant field set + persistent Items/Shipping/Total, `Idempotency-Key` reuse across retries, the full invoice state machine (creating/awaiting_payment/confirmed/expired/creation_unknown/cancelled), and never offers a second invoice after uncertainty (UI-01, WEB-02, 02-UI-SPEC §A2)."
    - "A3 order status reads the token from the URL fragment, strips it via `history.replaceState`, holds it in memory only, polls with `X-Order-Token`, renders only the §5.4 field set with buyer-facing labels, and gives an identical 'no longer valid' response for dead/wrong/nonexistent tokens (§5.4/§11.4, WEB-02)."
    - "B1 renders the Linear Split workspace: 390px searchable/filterable list + persistent detail, count summary, exception banner naming the exception, legal actions only (illegal hidden/disabled with reason), metrics grid, items+fulfillment, embedded order_events chronology, collapsed technical details (truncated payment_hash + relay ACK evidence only) (ORD-01, UI-02)."
    - "B2 publication health shows per-relay connection/ACK/pending evidence labeled 'delivery evidence', outbox state pills, partial publication naming missing relays, failed rows with retry, and clock-skew pause — never implying publication == settlement (PUB-02)."
    - "B6 appearance editor implements the three tiers (presets / Brand Basics / guarded Advanced Tokens opt-in), the Editorial/Guided/Compact layout-preset selector with the mobile-fallback note, live public-only preview, failing-pair contrast display, reset-to-preset, and the boundary note verbatim (UI-01, UI-03)."
    - "All UI-SPEC copy strings render verbatim; no secrets, nsec, bearer tokens, full payment hashes, or complete BOLT11 appear in any admin surface (buyer invoice view excepted for its own BOLT11) (02-UI-SPEC copy rules)."
    - "Public theme tokens never reach admin documents; checkout fields/totals/validation/payment states/security copy are invariant across themes and presets (UI-03, UI-01)."
  artifacts:
    - path: "gammamarkets/templates/gammamarkets/admin.html + static/gammamarkets/js/admin_app.js"
      provides: "host-shell admin app shell + sidebar nav + shared list/detail/fetch helpers"
    - path: "admin_orders.js / admin_catalog.js / admin_publications.js / admin_settings.js / admin_notifications.js"
      provides: "B1 order workspace, B3 catalog management, B2 publication health, B4 merchant settings, B5 notifications"
    - path: "public_product.html + public_storefront.js + public_checkout.js + public_order.html/.js"
      provides: "A2 adaptive checkout embedded in the A1 product page + A3 fragment-token order status per UI-SPEC"
    - path: "tests/runtime/test_admin_ui.py + test_buyer_ui.py + test_release_a_journey.py"
      provides: "admin/buyer surface evidence + the full Release-A end-to-end journey"
  key_links:
    - from: "public_checkout.js"
      to: "POST /public/checkout + GET /public/order-status"
      via: "Idempotency-Key reuse + X-Order-Token 5s polling through the invoice state machine"
      pattern: "X-Order-Token"
    - from: "public_order.js"
      to: "GET /public/order-status + POST /public/order-email-opt-out"
      via: "fragment → memory → header; replaceState strips the token"
      pattern: "history.replaceState"
    - from: "admin_orders.js"
      to: "§5.3 order routes"
      via: "legal-action map drives visible controls; exceptions carry only accept/refund/confirm-refund"
      pattern: "resolve-exception"
    - from: "admin_settings.js appearance editor"
      to: "themes token API (02-02)"
      via: "validated token object + server-side contrast gates; client live-preview uses the same math"
      pattern: "contrast"
---

## Phase Goal

**As a** buyer, **I want** a clear adaptive checkout and a private order page; **as a** merchant, **I want** one workspace for orders, catalog, publication health, notifications, and appearance — **so that** the whole Release-A loop is usable end to end per the approved UI contract.

(Story source: REQUIREMENTS.md PUB-02/ORD-01/NOTF-01/UI-01/UI-02/UI-03/WEB-01/WEB-02, Phase 2 goal criteria 1–2, 4 — the UI layer.)

<objective>
Build every UI surface on top of the completed APIs: the host-shell admin app (sidebar nav, shared helpers) with B1 order workspace, B3 catalog management, B2 publication health, B4 merchant settings, B5 notifications, B6 appearance + layout-preset selector; and the standalone public A2 checkout + A3 order-status pages with the fragment-token contract. Every surface implements the `02-UI-SPEC.md` component, state, copy, breakpoint, and accessibility contracts verbatim.

Backend is complete (02-01 domain, 02-02 publication/storefront, 02-03 order/payment APIs) — this plan is presentation + wiring + the end-to-end journey proof. The Phase 1 harness stays green (D-05).

Purpose: the user's explicit instruction — `.planning/sketches/` is the guideline; this plan is where every sketch decision (Adaptive Blend, Linear Split, Tiered Controls) becomes the shipping surface.

Output: admin + public templates/JS and three test suites including the full Release-A journey.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-release-a-safe-web-commerce/02-CONTEXT.md
@.planning/phases/02-release-a-safe-web-commerce/02-RESEARCH.md
@.planning/phases/02-release-a-safe-web-commerce/02-UI-SPEC.md
@.devin/skills/sketch-findings-gammamarkets/SKILL.md
@.devin/skills/sketch-findings-gammamarkets/references/buyer-experience.md
@.devin/skills/sketch-findings-gammamarkets/references/merchant-operations.md
@.devin/skills/sketch-findings-gammamarkets/references/theme-system.md
@.planning/sketches/MANIFEST.md
@docs/technical-specification.md

UI contract (binding — `02-UI-SPEC.md`): component inventory (host-vendored Quasar 2.22.0 + Vue 3.5.34 globals only — no npm/React/Tailwind), tokens (4 font sizes 14/16/24/36, 2 weights 400/600, spacing scale 4/8/12/16/24/32/48/64, 60/30/10 color, semantic tokens system-controlled), copywriting contract (every listed string verbatim incl. RFC 9457→friendly map), surface contracts A1–A3 + B1–B6 with all states (empty/error/loading/partial/long-text/overflow/populated), breakpoints (lg ≥1024, md 561–1023 [may collapse ≤860 if it reads better], sm ≤560 compact always wins), `.gm-public` public/admin separation, WCAG save gates, 44px public / 40px admin-dense touch targets, no icon-only mutators.

Surfaces implemented here: A2, A3, B1, B2, B3, B4, B5, B6 (A1's product document renders in 02-02; per UI-SPEC §A2 the checkout card is EMBEDDED in that document — this plan completes the checkout module in `public_product.html`/`public_storefront.js`/`public_checkout.js`; no standalone checkout page exists — §5.4 declares no such route). The 27-item probe mapping in UI-SPEC §UI-Considerations is the checklist — every item's resolution must be visible in the shipped surfaces.

Pinned data sources (W-NEW-1): B2 reads `GET /merchants/{id}/outbox` + `GET /merchants/{id}/relay-health` and retries via `POST /merchants/{id}/outbox/{intent_id}/retry` (declared 02-02 delta routes); B5 reads the `queue` array on `GET /merchants/{id}/notifications` + `POST .../notifications/test` (02-03); B6 reads/writes `theme` on `GET /merchants/current` + `PATCH /merchants/{id}` (02-02); B4 relay config + B6 layout preset write through `PATCH /merchants/{id}` (02-01); B3 uses §5.2; B1 uses §5.3; A2/A3 use §5.4.

Sketch guideline: `.planning/sketches/001-buyer-checkout/` (Adaptive Blend), `002-order-operations/` (Linear Split), `003-theme-controls/` (Tiered Controls) + `themes/` presets. Where the sketch and UI-SPEC differ, UI-SPEC wins; where both are silent, spec §5/§7 semantics win.

Spec sections the UI must honor: §5.3–5.4 (route payloads + restricted public field set), §5.6 (error copy map), §6.7 (deletion/deactivation flows + tombstone disclosures), §7.1–7.2 (legal action map), §8.3 (exception actions + refund attestation), §8.8 (notification states), §11.4 (token contract + reissue), §15 (bounds surfaced in forms), §16 (technical-evidence display limits), §21 decisions (payment_preference manual badge, refund honesty, NIP-17 "Not available in this release", deactivation two-step).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Admin shell + catalog (B3) + merchant settings (B4) + appearance (B6) (UI-01, UI-03)</name>
  <files>gammamarkets/__init__.py, gammamarkets/views.py, gammamarkets/templates/gammamarkets/admin.html, gammamarkets/static/gammamarkets/js/admin_app.js, gammamarkets/static/gammamarkets/js/admin_catalog.js, gammamarkets/static/gammamarkets/js/admin_settings.js, tests/runtime/test_admin_ui.py</files>
  <action>
1. `admin.html` + `admin_app.js`: `{% extends "base.html" %}` + `window_vars(user)`; sidebar nav Orders / Catalog / Publications / Settings; shared fetch helper (admin JSON + error→problem-details toast + inline field errors, never toast-only validation); host-vendored Vue/Quasar globals only, extension assets via `static_url_for`; responsive per UI-SPEC breakpoints (sidebar collapses to 72px icons at md, hidden at sm with overflow menu).

2. B3 catalog management per UI-SPEC §B3 against the 02-01 §5.2 API: products/collections/shipping dense QTables (title, d_tag, type/format, price, stock on_hand−reserved or "Unlimited", visibility pill, "Draft — never published" badge, published state); product editor (identity bounds, markdown description via allowlist sanitizer, ≤16 HTTPS image URLs + standing remote-image IP disclosure, specs, categories, weight/dims, stock, visibility, draft toggle, collection membership with publish-block-at-zero, shipping refs + extra-cost); variation editor (exactly one `a` parent, depth=1); shipping editor (service enum, pickup location/geohash, ISO alpha-2 countries, duration min≤max H|D|W, price-distance rejected); soft-delete dialog (no-guarantee copy + reference report or strip-and-republish choice); `GET /products/{id}/events` dry-run viewer (read-only unsigned event JSON).

3. B4 merchant settings per UI-SPEC §B4: identity card (display name, hex pubkey copyable, `payment_preference` "manual" badge, `SPEC_REVISION`); key generate/import (masked input, "Sent once over TLS and never displayed or logged.", never echoed); wallet selector with change-blocked reason when orders are `invoice_pending|awaiting_payment`; relay config list (wss:// validation errors inline); activation buttons ("Publish everything" → publication_pending flow); two-step "Begin deactivation" listing blocking nonterminal orders/exceptions first, tombstones commerce+NIP-89 but not kind-0/10050 by default (explicit key-retirement checkbox); topology-refusal QBanner when the readiness check reports unsupported topology; Gamma-orders channel shown "Not available in this release"; audit-posture warning banner surfaced when the 02-01 readiness check reports audit capture enabled.

4. B6 appearance per UI-SPEC §B6 — the Tiered Controls editor against the 02-02 theme API: tier 1 preset cards (Warm Market / Clean Minimal / High Contrast with swatch strips); tier 2 Brand Basics (display name, ≤3-char initials, primary accent, preset font-stack choice, corner character→radius); tier 3 Advanced Tokens behind the explicit opt-in gate + warning (bounded token list only); **the Editorial/Guided/Compact layout-preset selector lives here** (UI-01) with the verbatim note "Mobile always uses the compact layout — responsive safety overrides this preference."; live preview of PUBLIC surfaces only; contrast meter per gated pair; save blocked on gate failure with failing pair + computed ratio; "Reset to preset" per tier; the boundary note verbatim: "Appearance applies to your public storefront only. It never changes the admin area, checkout fields, totals, validation, or payment states."

5. `tests/runtime/test_admin_ui.py` (marker `runtime`): admin pages render inside host shell for the owner, 404/forbidden for others; every UI-SPEC copy string present verbatim (spot-check the full copywriting table via seeded states); catalog editor round-trips a product incl. markdown fidelity; dry-run viewer renders deterministic unsigned JSON; appearance editor rejects failing contrast pairs with named pair+ratio, requires the advanced opt-in, resets cleanly; layout-preset selector persists the choice; deactivation two-step lists blockers; topology + audit-posture banners render when fixture flags are set; no secret material (nsec, tokens, full payment hashes, complete BOLT11) in any admin HTML/JSON — seeded fixtures + response scans.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make lint</automated>
  </verify>
  <done>The host-shell admin app renders catalog management, merchant settings, and the tiered appearance editor per the contract — every copy string verbatim, WCAG gates enforced, layout preset selectable with the mobile-fallback note, and zero secret leakage in rendered surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Buyer checkout (A2) + order status (A3) (WEB-02, UI-01)</name>
  <files>gammamarkets/views.py, gammamarkets/templates/gammamarkets/public_product.html, gammamarkets/templates/gammamarkets/public_order.html, gammamarkets/static/gammamarkets/js/public_storefront.js, gammamarkets/static/gammamarkets/js/public_checkout.js, gammamarkets/static/gammamarkets/js/public_order.js, tests/runtime/test_buyer_ui.py</files>
  <action>
1. A2 checkout per UI-SPEC §A2 — the checkout card is embedded in the A1 product document (persistent card in `.product-layout`): complete the checkout module in `public_product.html` + `public_storefront.js` + `public_checkout.js` against the 02-03 public API (no standalone checkout route exists in §5.4): preset layouts (Editorial embedded card / Guided QStepper Product→Delivery→Pay / Compact progressive disclosure) driven by the merchant's stored layout preset; compact forced ≤560px regardless; invariant field set (country select, address iff physical, optional email, `email_opt_in` "Send transactional updates. No marketing."); persistent Items/Shipping/Total summary always visible pre-invoice; inline `aria-live="polite"` field validation with the contract's error copy; `Idempotency-Key` generated once per checkout session (`[A-Za-z0-9_-]{32,128}`, ≥128 random bits) and reused on retry — a 409 replay renders the existing order.

2. Invoice state machine on screen per the contract: `creating` (skeleton, no QR) → `awaiting_payment` (host vue-qrcode QR + copyable full BOLT11 — the buyer's own + integer sats + decoded-BOLT11 expiry countdown + "Waiting for payment" pill + "Invoice is correlated to this order only. Creating it does not mark the order paid.") → `confirmed` (success view + order ref + chronology) | `expired` ("Invoice expired — no payment was taken…" + "Review order again") | `creation_unknown` (payment-uncertain copy — NEVER a second invoice, no "pay again" affordance) | `cancelled`. 5s status polling with `X-Order-Token`. RFC 9457 codes map to the contract copy (`insufficient-stock`, `order-expired`, `rate-limited`, `invalid-shipping-destination`).

3. A3 order status per UI-SPEC §A3: `GET /gammamarkets/order` document; JS reads `#<token>` fragment, `history.replaceState` strips it immediately, token held in memory only, sent as `X-Order-Token` on 5s polls until terminal state; renders only the §5.4 field set with buyer-facing labels ("Waiting for payment" / "Payment confirmed" / "Preparing your order" / "Complete" / "This order is no longer active" / "On hold — the merchant is reviewing a payment issue."); dead/revoked/expired → "This order link is no longer valid."; "Copy status link" + "Stop order emails" actions; loading = last-known state + "checking…" indicator, never spinner-only; npub/order-ref truncate-middle with copy affordance.

4. `tests/runtime/test_buyer_ui.py` (marker `runtime`): checkout document carries the required headers + CSP; invariant elements present in each preset (field set, summary, totals) — assert structural parity across presets; compact structure forced at sm viewport; idempotency-key generation shape + reuse on retry; invoice states render correct copy (fixture-driven via API stubs or seeded orders at each state); order page JS contains the fragment→replaceState→header flow (static assertion on shipped JS + behavior test where feasible); no token in any emitted URL/link; no merchant internals in the pages; reduced-motion + `:focus-visible` affordances present (per UI-SPEC backstops).
  </action>
  <verify>
    <automated>make host && make verify-runtime && make lint</automated>
  </verify>
  <done>Buyers get the complete Adaptive Blend checkout with all payment states + honest uncertainty handling, and the fragment-token order page with the strict §5.4/§11.4 privacy contract — all copy verbatim, all invariants held across presets and breakpoints.</done>
</task>

<task type="auto">
  <name>Task 3: Order workspace (B1) + publication health (B2) + notifications (B5) + Release-A journey (PUB-02, ORD-01, NOTF-01, UI-02)</name>
  <files>gammamarkets/static/gammamarkets/js/admin_orders.js, gammamarkets/static/gammamarkets/js/admin_publications.js, gammamarkets/static/gammamarkets/js/admin_notifications.js, tests/runtime/test_admin_ui.py, tests/runtime/test_release_a_journey.py</files>
  <action>
1. B1 order workspace per UI-SPEC §B1 against §5.3: `.workspace` grid `390px minmax(0,1fr)` (320px at md); filters row (search "Search order or buyer" + state select All/Awaiting payment/Confirmed/Needs attention/Expired/Cancelled); count summary "N active orders · M need attention"; rows (reference, channel chip — Web only in Release A but render Gamma/NIP-15 chips correctly if data ever carries them, buyer handle, item summary, total sats, state pill, active-row left rule + 10% tint); selection survives filter changes; detail pane fixed order — heading (ref/channel/buyer/state) → exception banner (danger, names the exception) → legal contextual actions only (illegal hidden/disabled + tooltip reason) → metrics grid (Total/Payment/Inventory/Protocol) → items + fulfillment (decrypted address, owner only) → QTimeline `order_events` chronology → collapsed "Technical delivery details" (truncated payment_hash + relay ACK evidence — never keys/full BOLT11/tokens). Legal action map per contract: `confirmed`→Start processing/Cancel with reason; `processing`→Mark shipped→Mark delivered; `awaiting_payment`/`invoice_pending`→Cancel; `payment_exception`→Accept paid order (oversell disclosed)/Request refund/Confirm refund sent; `expired|cancelled`+settled→accept via exception only; terminal→disabled "No legal action". Cancel QDialog requires reason + refund warning copy. "Reissue status link" flow (old revoked immediately, new shown once). Mobile ≤560px: list→detail with "← Back to orders". Empty/loading/error/populated/partial/overflow states per contract copy.

2. B2 publication health per UI-SPEC §B2 against the pinned sources — `GET /merchants/{id}/relay-health` + `GET /merchants/{id}/outbox` (02-02 delta route): per-relay table (URL, connection state from transport health tick, last positive/negative ACK, pending counts); outbox QTable dense (kind, aggregate ref, state pill pending|claimed|publishing|partially_published|failed|superseded, attempts, per-relay outcome cells, supersession marker — `order_msg` cells render honestly even though none exist in Release A); empty → "No publications pending"; partial names non-ACKed relays; failed → retry affordance via `POST /merchants/{id}/outbox/{intent_id}/retry`; clock-skew pause surfaced as health failure; "delivery evidence" labeling verbatim; payment-exceptions attention list (populated from orders API `payment_exception` rows).

3. B5 notifications per UI-SPEC §B5 against the pinned sources — `GET|PATCH /merchants/{id}/notifications` (incl. the `queue` payload array from 02-03) + `POST .../notifications/test`: ≤5 validated addresses, per-event toggles (default order_received/confirmed/on_hold), "Send test" ≤5/hour (backend bucket enforced in 02-03), per-recipient queue states sent/pending/suppressed(reason)/failed-after-N — honest, no transient-vs-5xx invention; customer-email explainer (per-order opt-in, transactional only, link is a bearer token — disclosed).

4. `tests/runtime/test_admin_ui.py` (extend): B1 renders the full state/exception matrix from seeded orders (each state pill, exception banner, legal-action sets per state, disabled-with-reason on illegal); workspace responsive collapse at breakpoints; B2 renders empty/partial/failed/published + per-relay cells + "delivery evidence" labels; B5 renders each queue state; every contract copy string for B1/B2/B5 present; no secrets/tokens/full payment evidence in any admin render.

5. `tests/runtime/test_release_a_journey.py` (marker `runtime`): the complete Release-A journey through real surfaces — merchant setup → catalog → publish intents → outbox publishes to LocalRelay with evidence → buyer loads themed product page → checkout (real POST) → invoice view → FakeWallet settlement → status polls to confirmed → merchant B1 shows the order → admin actions drive transitions → email queue rows exist per recipient → order-status page serves the buyer view. Assert at UI level where practical (rendered HTML/JS behavior) and API level otherwise; this is the phase's capstone integration test.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>All nine UI-SPEC surfaces ship per contract; the Release-A journey passes end to end through real admin + public surfaces with real settlement; every copy, state, breakpoint, separation, and privacy requirement is exercised by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public JS -> public API | token stays in memory/header; fragment never reaches the server |
| admin JS -> admin API | owner-scoped + origin/CSRF guard; legal actions only rendered |
| theme editor -> token store | client preview uses the same contrast math; server gates authoritative |
| rendered surfaces -> observers | no secrets/tokens/internal ids in HTML/JSON — scanned in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-204-01 | Information disclosure | token leaking via fragment/history/referrer | critical | mitigate | replaceState strip + in-memory only + no-referrer + tests asserting absence |
| T-204-02 | Tampering | UI offering illegal transitions | high | mitigate | legal-action map renders only contract-legal controls; API rejects regardless (02-03) |
| T-204-03 | Information disclosure | admin render leaking secrets | high | mitigate | response scans over seeded fixtures; technical-details pane shows truncated evidence only |
| T-204-04 | Elevation | theme tokens styling admin | medium | mitigate | `.gm-public` scoping asserted; admin never receives token object |
| T-204-05 | Denial of service | buyer confusion into double payment | critical | mitigate | creation_unknown copy forbids second invoice; no pay-again affordance; settlement copy only after LNbits confirm |
</threat_model>

<verification>
- `make verify-runtime` — admin/buyer/journey suites green on the selected dialect.
- `make verify` — entire suite green (Phase 1 + all runtime).
- `make lint` — ruff clean.
- Journey test is the Release-A capstone — it must pass on both dialects in the blocking CI matrix.
</verification>

<success_criteria>
- Buyer UI: adaptive checkout + private order status with all payment states, invariants, and copy (WEB-01/02, UI-01).
- Merchant UI: order workspace, publication health, catalog, settings, notifications, appearance — all per contract (ORD-01, PUB-02, NOTF-01, UI-01/02/03).
- Theme separation, WCAG gates, breakpoints, and security copy hold everywhere.
- The Release-A journey passes end to end — phase goal criteria 1–5 all demonstrable.
</success_criteria>

<output>
Create `.planning/phases/02-release-a-safe-web-commerce/02-04-SUMMARY.md` when done.
</output>
