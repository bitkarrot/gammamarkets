---
phase: 02-release-a-safe-web-commerce
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gammamarkets/__init__.py
  - gammamarkets/config.json
  - gammamarkets/db.py
  - gammamarkets/migrations.py
  - gammamarkets/settings.py
  - gammamarkets/crypto.py
  - gammamarkets/keystore.py
  - gammamarkets/security.py
  - gammamarkets/models.py
  - gammamarkets/services/__init__.py
  - gammamarkets/services/merchant.py
  - gammamarkets/services/catalog.py
  - gammamarkets/services/events.py
  - gammamarkets/views_api.py
  - gammamarkets/templates/gammamarkets/index.html
  - gammamarkets/static/gammamarkets/probe.txt
  - tests/conftest.py
  - tests/runtime/conftest.py
  - tests/runtime/test_install.py
  - tests/runtime/test_crypto_keystore.py
  - tests/runtime/test_merchant_api.py
  - tests/runtime/test_catalog_api.py
  - Makefile
autonomous: true
requirements:
  - MERC-01
  - CAT-01
  - CAT-02
  - SEC-01

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "The `gammamarkets` package is discovered, migrated, and route-mounted by the real pinned host loader when placed under `LNBITS_EXTENSIONS_PATH` — no mocks of the extension machinery (D-03, MERC-01)."
    - "`gammamarkets_start` is a synchronous `def` doing only bounded `task_manager` registrations; `gammamarkets_stop` cancels only owned handles (spec §10, research Pitfall 6)."
    - "Missing, duplicate, short, or malformed `GAMMAMARKETS_MASTER_KEYS`/`GAMMAMARKETS_ACTIVE_KEY_VERSION`/`GAMMAMARKETS_PRIVACY_KEY`/`GAMMAMARKETS_PUBLIC_BASE_URL` fail startup validation before merchant services activate (§12)."
    - "Merchant nsecs exist only as AES-256-GCM ciphertext with §11.2 length-prefixed AAD; no code path logs, renders, or returns raw key material; equality lookups use purpose-separated HMAC-SHA256 under `GAMMAMARKETS_PRIVACY_KEY` (§11.2–11.4, SEC-01)."
    - "Every admin mutation route rejects user-id-only auth and cross-origin/missing-Origin cookie requests; merchant data is scoped by `user.id` AND merchant id (§5.1, SEC-01)."
    - "Catalog writes enforce §4.2–4.6 bounds (title ≤200, summary ≤500, description ≤64KB sanitized markdown, ≤16 HTTPS images, stock NULL=unlimited, variation depth=1, collection publish requires ≥1 active member, shipping `price-distance` rejected) and soft-delete with ordered reference removal + tombstone intent (§6.7, CAT-01/CAT-02)."
    - "Catalog/merchant mutations enqueue outbox intent rows in the same transaction as the domain write, and unsigned kind 0/30402/30405/30406/31989/31990 event builders are deterministic for identical domain state (§6, §8.6)."
    - "Markdown/JSON payloads stored via `insert`/`update` (or pre-encoded) never pass through `rewrite_values`-stripped raw `execute` params — round-trip fidelity is tested (research Pitfall 3)."
    - "SQLite multi-process and CockroachDB topologies are refused with an explicit readiness failure and admin banner (§14, decision 15, SEC-01)."
  artifacts:
    - path: "gammamarkets/__init__.py"
      provides: "gammamarkets_ext APIRouter(prefix='/gammamarkets'), gammamarkets_static_files declaration, sync gammamarkets_start, gammamarkets_stop, db re-export"
    - path: "gammamarkets/config.json"
      provides: "extension manifest discovered by the host loader (name, version, min_lnbits_version, tile, permissions)"
    - path: "gammamarkets/migrations.py"
      provides: "m001 covering merchants, merchant_keys, settings, catalogs, products + §4.4 detail tables, collections + collection_shipping, shipping_options, protocol_addresses, relay_configs, outbox_events, outbox_dependencies, relay_publications, task_leases, rate_limit_buckets + §4.19 indexes for these tables"
    - path: "gammamarkets/crypto.py"
      provides: "AES-256-GCM versioned envelope, length-prefixed AAD, HMAC equality indexes, §11.4 public-token hash/store/compare"
    - path: "gammamarkets/keystore.py"
      provides: "MerchantKeyStore local backend: generate/import/public_key/sign_event/delete/rewrap; nip17_wrap/nip17_unwrap/nip04_* present and explicitly gated to Release B/C"
    - path: "gammamarkets/services/merchant.py"
      provides: "1:1 merchant-per-user domain, wallet binding + ownership revalidation, activation state machine, relay config, notify_emails"
    - path: "gammamarkets/services/catalog.py"
      provides: "catalog/product/collection/shipping CRUD, validation, drafts, soft-delete + tombstone intents, same-tx outbox enqueue"
    - path: "gammamarkets/services/events.py"
      provides: "deterministic unsigned builders for kinds 0/30402/30405/30406/31989/31990 + kind-5 tombstone intents per §6"
    - path: "gammamarkets/views_api.py"
      provides: "§5.1 merchant routes and §5.2 catalog routes under /gammamarkets/api/v1 with the security dependency"
    - path: "tests/runtime/conftest.py"
      provides: "host fixture that installs the real package + config.json into a temp LNBITS_EXTENSIONS_PATH before create_app()"
    - path: "tests/runtime/test_install.py,test_crypto_keystore.py,test_merchant_api.py,test_catalog_api.py"
      provides: "runtime evidence for install/migration/lifecycle, crypto/key custody, merchant API, catalog API"
  key_links:
    - from: "gammamarkets/services/catalog.py"
      to: "gammamarkets/migrations.py outbox tables"
      via: "domain mutations enqueue outbox_events rows inside the same extension transaction"
      pattern: "outbox_events"
    - from: "gammamarkets/keystore.py"
      to: "gammamarkets/crypto.py"
      via: "local backend encrypts/decrypts nsec material through the versioned AEAD envelope"
      pattern: "key_version"
    - from: "gammamarkets/views_api.py"
      to: "gammamarkets/security.py"
      via: "every admin mutation Depends on the origin/CSRF/auth guard"
      pattern: "Depends"
    - from: "gammamarkets/services/events.py"
      to: "harness/registry.py state/route literals"
      via: "event kinds, d_tag rules, and §6.8 metadata must match the spec literals the closure gate extracts"
      pattern: "30402"
---

## Phase Goal

**As a** merchant, **I want to** create a protected identity, bind my wallet, and manage my canonical catalog on real infrastructure, **so that** everything downstream (publication, checkout, settlement) operates on authoritative, encrypted-at-rest domain state.

(Story source: REQUIREMENTS.md MERC-01/CAT-01/CAT-02, Phase 2 goal criterion 1, first half.)

<objective>
Stand up the production `gammamarkets` LNbits extension inside this repository (D-03): the host-discoverable package skeleton, extension database with `m001` migrations for the merchant/catalog/outbox-intent schema subset, versioned key custody and field-level crypto, the merchant domain service (identity, wallet binding, activation state, relays, notifications config), the full catalog domain (catalogs, products, collections, shipping, drafts, soft-delete), deterministic unsigned event builders, and the §5.1/§5.2 admin JSON API — all behind the extension-owned origin/CSRF/auth guard, on the pinned host.

This plan produces no buyer-facing surface and no relay publication worker — outbox rows are enqueued transactionally here and published by plan 02-02; orders/payments land in plan 02-03. The Phase 1 harness (`make verify`) must stay green throughout (D-05).

Purpose: every later capability (NIP-99 publication, checkout, order admin) reads/writes these tables and services; landing the domain and custody layer first with real-loader evidence removes the highest-risk host-convention unknowns (install path, migration runner, template resolution, sync-start contract) before feature work depends on them.

Output: the `gammamarkets/` package, `tests/runtime/` suite, and one admin shell template proving the template-renderer convention.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-release-a-safe-web-commerce/02-CONTEXT.md
@.planning/phases/02-release-a-safe-web-commerce/02-RESEARCH.md
@.planning/phases/02-release-a-safe-web-commerce/02-UI-SPEC.md
@docs/technical-specification.md
@PINS.md

Normative spec sections this plan implements: §3.1–3.3 (identifiers), §4.1–4.6 + §4.10–4.11 + §4.13–4.17 + §4.19 (schema subset), §5.1–5.2 (admin API), §5.6 (RFC 9457 error model), §6.1–6.5 + §6.7–6.8 (event construction incl. tombstones/drafts/self-describing metadata), §7.4 (outbox states as enqueue targets), §10 (start/stop contract — registration only, workers registered in later plans), §11 (key custody, field encryption, HMAC indexes, public tokens), §12 (configuration), §14 (transactions, topology refusal), §15 (input bounds), §19 (threat model), §21 (decisions register).

Research findings that are binding: sync `gammamarkets_start` (Pitfall 6); `Connection.execute` auto-commits — all multi-statement writes go through the §14 adapter pattern from `harness/tx.py` (Pitfall 2); `rewrite_values` HTML-strips `execute`/`fetch*` params — persist markdown/JSON via `insert`/`update` or pre-encoded payloads (Pitfall 3); one `Database.connect()` serializes — expose a per-worker `Database("ext_gammamarkets")` handle factory (Pitfall 4); no host CSRF + CORS `*` → extension-owned Origin enforcement (Pitfall 7); host audit middleware can log path/query/body — secrets/tokens only ever travel in headers or request bodies of routes documented for redaction (Pitfall 8); `gammamarkets_static_files` declaration is host-mounted (Open Question 1 — the PINS ban covers extension code instantiating `FileResponse`/`StaticFiles`, not the declarative list; record the disposition in the summary).

Open questions this plan resolves and must document in the summary: OQ1 (static-files declaration disposition — declared per convention, recorded in summary), OQ2 (template-name ↔ on-disk layout — spike-render `index.html` through the real loader in Task 1), OQ3 (audit-middleware capture — Task 2 step 4b deployment-level disposition), OQ4 (whether `config.json` `permissions` has runtime effect for Python extensions), OQ5 (install mechanics — temp `LNBITS_EXTENSIONS_PATH` + real package copy), OQ7 (migration split — `m001` here, `m002` in 02-03, none in 02-02).

Harness reuse: `harness/tx.py` dialect transaction patterns, `harness/authprobe.py` origin/token probes, and `harness/registry.py` spec literals are the executable references — the runtime adapter, guard, and state/enumeration literals must match them. `tests/conftest.py` gains one `"runtime"` marker line; `Makefile` gains `verify-runtime` (`pytest tests/runtime -q` with SUBSET_OK); nothing else in the harness is touched.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extension package skeleton, real-loader install fixture, m001 schema (MERC-01 groundwork)</name>
  <files>gammamarkets/__init__.py, gammamarkets/config.json, gammamarkets/db.py, gammamarkets/migrations.py, gammamarkets/models.py, gammamarkets/services/__init__.py, gammamarkets/templates/gammamarkets/index.html, gammamarkets/static/gammamarkets/probe.txt, tests/conftest.py, tests/runtime/conftest.py, tests/runtime/test_install.py, Makefile</files>
  <action>
1. `gammamarkets/` package per the research-verified layout: `__init__.py` defines `gammamarkets_ext = APIRouter(prefix="/gammamarkets", tags=["gammamarkets"])`, includes the (initially minimal) generic + API routers, declares `gammamarkets_static_files = [{"path": "/gammamarkets/static", "name": "gammamarkets_static"}]`, re-exports `db`, and defines synchronous `gammamarkets_start()` (bounded `task_manager` registrations only — initially just the readiness/lease structure; workers are registered by 02-02/02-03) and `gammamarkets_stop()` cancelling exactly the owned handles. `config.json` carries name/version/short_description/tile/min_lnbits_version/permissions. `db.py` exposes `Database("ext_gammamarkets")` plus a per-worker handle factory (new `Database` per concurrent worker — Pitfall 4). `models.py` holds pydantic models for the m001 tables.

2. `migrations.py` `m001_initial(db)`: full DDL per spec §4 for `merchants`, `merchant_keys`, `settings`, `catalogs`, `products`, `product_images`, `product_specs`, `product_categories`, `product_collections`, `product_shipping_options`, `product_shipping_collections`, `collections`, `collection_shipping`, `shipping_options`, `protocol_addresses`, `relay_configs`, `outbox_events`, `outbox_dependencies`, `relay_publications`, `task_leases`, `rate_limit_buckets` — using `db.serial_primary_key`/`big_int`/`blob`/`timestamp_now`/`references_schema` compat helpers and `:named` params, plus the §4.19 indexes that touch these tables. Column names/constraints must be literal to §4 — the registry in `harness/registry.py` is the reference. Migration split (OQ7): `m001` here covers merchant/catalog/outbox-intent tables; `m002` (02-03) adds the orders/payments/inventory/idempotency/email cluster plus schema-only `inbox_events`/`order_messages` (keeping §4 whole; they stay unused until Release B); `peer_relays`, `relay_cursors`, and `migration_jobs` defer to Phase 3/4 migrations (record this in the summary).

3. `tests/runtime/conftest.py`: `ext_host` fixture — copy the `gammamarkets/` package + `config.json` into a fresh tmp `LNBITS_EXTENSIONS_PATH` before `create_app()` (reusing `harness/host.py` patterns: FakeWallet, tmp data folder, LifespanManager), plus an httpx ASGI client factory and a merchant-user+wallet helper. This exercises real discovery, `run_migration`, route mount, and start-hook invocation end to end (OQ5).

4. Template spike (OQ2): minimal `templates/gammamarkets/index.html` extending `base.html` rendered by one admin `GET /gammamarkets/` route through `template_renderer(["gammamarkets"])`; assert HTTP 200 + expected content. Record the verified template-name↔path rule in the summary. Also verify `config.json` permissions parse and note any runtime enforcement observed (OQ4).

5. `tests/conftest.py`: add `"runtime"` marker; `Makefile`: add `verify-runtime` target (`uv run pytest tests/runtime -q; $(SUBSET_OK)`).

6. `tests/runtime/test_install.py` (marker `runtime`): extension discovered + active under the tmp extensions path; m001 ran (tables exist in `ext_gammamarkets` schema/file, `dbversions` recorded); `/gammamarkets/` returns 200 for an authed user; static mount serves a probe file at `/gammamarkets/static/...`; `gammamarkets_start` ran synchronously at startup (assert registered task names present); deactivation via settings 404s the `/gammamarkets` top path (middleware contract); a second boot is idempotent (no duplicate migration, no route errors).
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify-fast && make lint</automated>
  </verify>
  <done>The package installs through the real host loader: auto-discovered via config.json, m001 applied to the `ext_gammamarkets` database, routes mounted at `/gammamarkets`, sync start/stop honored, template rendering path proven — all asserted by tests/runtime against the pinned host app, with OQ2/OQ4/OQ5/OQ7 resolutions recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Configuration validation, field-level crypto, MerchantKeyStore, merchant domain + §5.1 API (MERC-01, SEC-01)</name>
  <files>gammamarkets/settings.py, gammamarkets/crypto.py, gammamarkets/keystore.py, gammamarkets/security.py, gammamarkets/services/merchant.py, gammamarkets/views_api.py, tests/runtime/test_crypto_keystore.py, tests/runtime/test_merchant_api.py</files>
  <action>
1. `settings.py`: extension-owned `GAMMAMARKETS_*` settings module (own BaseSettings/env parse — the host has no extension-settings hook). Strict startup validation per §12: `GAMMAMARKETS_MASTER_KEYS` parses as a versioned keyring map of 32-byte base64 keys (missing/duplicate/short/malformed → hard fail), `GAMMAMARKETS_ACTIVE_KEY_VERSION` must name a present key, `GAMMAMARKETS_PRIVACY_KEY` is a distinct required 32-byte secret, `GAMMAMARKETS_PUBLIC_BASE_URL` is a required canonical HTTPS origin (never derived from Host/Forwarded). Also parse §12 tunables (`RESERVATION_TTL`, `OUTBOX_MAX_ATTEMPTS`, `OUTBOX_BATCH`, `CHECKOUT_RATE_LIMIT`, `GAMMAMARKETS_EMAIL_ENABLED`, `EMAIL_MAX_ATTEMPTS`, `SPEC_REVISION`) with documented defaults. Validation failure must surface before merchant services activate (§12) — wire it into startup readiness.

2. `crypto.py`: §11.2 AES-256-GCM envelope — `key_version || random 96-bit nonce || ciphertext+tag` using the host's existing `pycryptodomex`; AAD = unambiguous length-prefixed encoding of `"gammamarkets"`, record id, table, column, key version (reject cross-record transplant in tests). §11.3 HMAC-SHA256 equality indexes under `GAMMAMARKETS_PRIVACY_KEY` over length-prefixed purpose + merchant id + normalized value with the spec's purpose names (`buyer-pubkey`, `order-id`, `wallet-id`, `source-wallet-id`, `client-ip`, `email-recipient`). §11.4 public tokens: 256-bit base64url generation, SHA-256 lookup hash compared with `hmac.compare_digest`, malformed/noncanonical rejection before lookup, AEAD-encrypted copy semantics (retained only while valid), immediate hash invalidation + encrypted-copy erasure on rotate/revoke/expire.

3. `keystore.py`: `MerchantKeyStore` local backend per §11.1–11.2 — `generate`, `import_key` (bech32 nsec decode, validate, encrypt into `merchant_keys` with `key_origin`), `public_key`, `sign_event` (decrypt inside the operation, sign via nostr-sdk `Keys`, release references promptly — never cache raw nsecs), `delete`, `rewrap` (resumable rotation scaffold honoring old+new keyring). `nip17_wrap`/`nip17_unwrap`/`nip04_decrypt`/`nip04_encrypt` exist on the interface but raise a clear `ReleaseNotAvailable` error — Release B/C scope (CONTEXT deferred list).

4. `security.py`: the admin-mutation guard dependency per §5.1 — reject user-id-only auth on mutations; cookie auth requires exact `Origin == GAMMAMARKETS_PUBLIC_BASE_URL` plus a per-session double-submit CSRF token; missing/`null` Origin fails cookie mutations; bearer auth passes without Origin requirement. Admin mutations also **accept** an `Idempotency-Key` header per §14 (admin clients SHOULD send one; accepted keys go through the same idempotency_records claim path as checkout). Plus the §5.6 RFC 9457 problem-details helpers (`urn:gammamarkets:*` codes) used by all API routes.

4b. **OQ3 disposition (spec §5 audit-capture MUST):** the host `AuditMiddleware` can record path params, query params, and request bodies verbatim with no redaction hook. The compliant disposition is deployment-level: qualified deployments MUST keep `lnbits_audit_log_request_body`, `lnbits_audit_log_query_params`, and `lnbits_audit_log_path_params` disabled (or the audit settings off entirely) for any deployment serving gammamarkets. Implement: (a) a startup readiness warning that inspects the live `settings.lnbits_audit_log_*` flags and surfaces a blocking admin banner + structured log when body/query/path capture is enabled — never silently pass; (b) the same check asserted in `test_merchant_api.py` — fixture runs with audit capture disabled (the qualified posture) and a companion test flips the flags on and asserts the warning fires; (c) the posture documented in the plan summary + PINS-adjacent operator note. This resolves research Open Question 3.

5. `services/merchant.py` + `views_api.py` §5.1 routes: `POST /merchants` (1:1 per user — second create rejected), `GET /merchants/current`, `PATCH /merchants/{id}` (profile, `payment_preference` fixed `manual`, wallet binding — verify the wallet exists, belongs to this user, `can_receive_payments`; wallet change blocked while any order is `invoice_pending|awaiting_payment`), `POST /merchants/{id}/keys/import` (masked nsec → keystore; body never logged), `POST /merchants/{id}/publish` (enqueue republication intents), `GET /merchants/{id}/relay-health` (stub-backed shape now — populated by 02-02 evidence), `GET|PATCH /merchants/{id}/notifications` + `POST /notifications/test` (queue a test row; ≤5 addresses, validated), `DELETE /merchants/{id}` (two-step: report blocking nonterminal orders first). Activation state machine `draft → publication_pending → active → deactivating → inactive` enforced in the domain layer. Relay config CRUD rides the declared `PATCH /merchants/{id}` route as a `relay_configs` payload field (validated list replaces the set transactionally) — no new route surface — enforcing the full §9.5 URL validation set — wss:// scheme only; reject loopback/private/link-local/multicast/reserved/metadata ranges (IPv4+IPv6), raw-IP hosts, userinfo, fragments, and Unicode-confusable hostnames; normalized form stored — implemented as a shared validator in `security.py` (reused by 02-02's relay service on every write and revalidate-on-connect). Unsupported-topology refusal (§14/decision 15): detect SQLite-multi-process/CockroachDB at startup and expose the blocking state to `GET /merchants/current` for the admin banner.

6. `tests/runtime/test_crypto_keystore.py` (marker `runtime`): envelope round-trip + tamper rejection + wrong-AAD rejection; wrong-version decrypt fails; keyring validation matrix (missing/dup/short/malformed/active-version-missing); HMAC purposes don't collide; token lifecycle (hash lookup, malformed reject, revoke erases copy, expired rejects); imported nsec never appears in `merchants`, logs (captured), or any API response. **§17 drill:** resumable master-key rotation — add a second key version, `rewrap` a merchant key + field ciphertexts mid-set (kill and resume the rewrap job halfway, assert zero rows remain on the old version only after full completion; old-key removal is blocked while old-version rows exist); backup/restore drill — snapshot the extension DB, wipe, restore, assert keys decrypt and HMAC lookups still resolve.

7. `tests/runtime/test_merchant_api.py` (marker `runtime`, runs under `ext_host`): full §5.1 flow — create (generate + import paths), current, patch profile/wallet (mismatch wallet → `wallet-mismatch`), duplicate merchant rejected, notifications patch + test-send queue row, relay config validation (each §9.5 rejection class: non-wss scheme, loopback/private/link-local/metadata IPs v4+v6, raw-IP, userinfo, fragment, confusable hostname), deactivation step-1 blocking report; security probes mirroring `harness/authprobe.py`: user-id-only auth rejected on mutations, cookie+missing-Origin rejected, cookie+wrong-Origin rejected, bearer accepted; another user's merchant id → 404/unauthorized (owner scoping); nsec-import body absent from audit-visible surfaces.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make lint</automated>
  </verify>
  <done>Merchant identity/wallet/relay/notification configuration works end to end over the real host with encrypted key custody, strict `GAMMAMARKETS_*` validation, owner-scoped + origin/CSRF-guarded admin mutations, RFC 9457 errors, and no raw-key or token exposure anywhere observable.</done>
</task>

<task type="auto">
  <name>Task 3: Catalog domain, deterministic event builders, §5.2 API (CAT-01, CAT-02)</name>
  <files>gammamarkets/services/catalog.py, gammamarkets/services/events.py, gammamarkets/views_api.py, tests/runtime/test_catalog_api.py</files>
  <action>
1. `services/catalog.py`: full §4.2–4.6 domain — catalogs; products (`simple|variable|variation` × `digital|physical`); collections; shipping options. Validation per spec + §15 bounds: title ≤200, summary ≤500, description ≤64KB markdown through the allowlist sanitizer, ≤16 HTTPS-only image URLs (no server-side fetch), `(amount_minor, currency, decimals)` prices, `stock_on_hand` NULL = unlimited, visibility `on-sale|hidden|pre-order` (pre-order discovery-only, not purchasable), `draft` flag (never published in v1). Variation rules: exactly one `a` parent link, depth=1 (a variation cannot parent). Collection publish blocked at zero active members (§6.1/§6.2). Shipping: service enum (standard/express/overnight/pickup — pickup requires location or geohash), non-empty ISO alpha-2 countries, duration min≤max with H|D|W unit, `price-distance` rejected in v1 (§6.3). Persistence discipline: all markdown/JSON fields written via `insert`/`update` or pre-encoded — never raw `execute` params subject to `rewrite_values` stripping (Pitfall 3); assert round-trip fidelity of `<tag>`-containing descriptions in tests.

2. Soft-delete per §6.7 (CAT-02): DELETE is always soft — `deleted_at` set, ordered reference removal, tombstone intent enqueued; when still referenced, surface the reference report or explicit strip-and-republish choice; removing a collection's last active product offers tombstone-or-replacement. `protocol_addresses` rows survive soft deletion so tombstones reference the latest event id.

3. Same-transaction outbox enqueue: every publishable mutation (create/update/visibility/stock-affecting? — per §8.6 the publisher rebuilds from current state, so intents carry aggregate_type/aggregate_id/aggregate_revision/event_kind) inserts an `outbox_events` row in `pending` inside the domain transaction, with `outbox_dependencies` expressing §8.6 ordering (30406 → 30405 → 30402; membership/reference changes enqueue every affected collection and product; deletion republishes survivors before the kind-5 tombstone). Supersession marking on newer revision follows §8.6 step 2.

4. `services/events.py`: deterministic unsigned builders per §6.1–6.5 + §6.8 — kind 30402 product (content/tags incl. `d`, price, stock, images, specs, shipping refs, `org.gammamarkets.protocol` namespace metadata), 30405 collection, 30406 shipping option, kind 0 merchant profile, 31989/31990 NIP-89 handler pair, kind-5 tombstone intent descriptors. Identical domain state → identical unsigned event bytes (deterministic tag ordering, fixed serialization); `created_at` assigned at publish time (02-02), not build time.

5. `views_api.py` §5.2 routes: `GET|POST /catalogs`, `GET|PATCH|DELETE /catalogs/{id}`, `GET|POST /products`, `GET|PATCH|DELETE /products/{id}`, `POST /products/{id}/images`, `GET|POST|PATCH|DELETE /collections[/{id}]`, `GET|POST|PATCH|DELETE /shipping[/{id}]`, `GET /products/{id}/events` (dry-run: rendered unsigned event JSON per protocol). All behind the Task-2 guard + owner scoping; RFC 9457 errors.

6. `tests/runtime/test_catalog_api.py` (marker `runtime`): CRUD + every validation bound (over-limit title/summary/description, >16 images, non-HTTPS image, bad currency decimals, price-distance rejection, pickup without location, bad ISO country, duration min>max); variation depth/parent rules; collection publish block at zero members; draft never enqueues publish intent; soft-delete ordering + tombstone intent + reference report; same-tx outbox rows visible with correct aggregate refs and dependency edges; dry-run determinism (two renders byte-identical) + correct kinds/tags/`org.gammamarkets.protocol` metadata; markdown round-trip fidelity through the API; owner scoping (user B cannot see/mutate user A's catalog).
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>Catalog management is fully functional over the admin API with all §4/§6/§15 bounds enforced, soft-delete + tombstone intents correct, publishable mutations transactionally enqueueing ordered outbox intents, deterministic unsigned event builders powering the dry-run route, and the complete suite (`make verify`) still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API -> extension db | owner-scoped queries only; bearer/cookie+Origin auth gate at route boundary (host CORS is `*`) |
| keystore -> merchant_keys | raw nsec exists only inside keystore operations; AEAD envelope at rest |
| request bodies -> host audit/log | secrets travel only in documented bodies; nothing secret in path/query ever |
| extension -> host Database layer | `execute` auto-commit and `rewrite_values` stripping are failure modes; adapter + insert/update discipline enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-201-01 | Information disclosure | nsec import / key storage | high | mitigate | AES-256-GCM versioned envelope + AAD; masked input; never logged/echoed; tests capture logs and assert absence |
| T-201-02 | Elevation | cookie-auth mutation via cross-origin | high | mitigate | Extension-owned Origin==PUBLIC_BASE_URL + double-submit CSRF + user-id-only rejection on every admin mutation; runtime probes mirror P0-12 |
| T-201-03 | Tampering | domain write without outbox intent | high | mitigate | same-transaction enqueue; tests assert intent rows exist per mutation |
| T-201-04 | Information disclosure | markdown/JSON mangled or injected | medium | mitigate | `insert`/`update` persistence + allowlist sanitizer + round-trip fidelity tests |
| T-201-05 | Denial of service | oversized catalog payloads | medium | mitigate | §15 bounds enforced at API boundary with RFC 9457 errors |
| T-201-06 | Elevation | unsupported topology silently running | medium | mitigate | startup topology check (§14) fails readiness; admin banner surfaces it |
</threat_model>

<verification>
- `make verify-runtime` — new runtime suite green on the selected dialect.
- `make verify` — full suite including Phase 1 qualification still green; P0-14 closure unaffected (registry is spec↔harness, runtime code doesn't perturb it — verify no module-name collisions with `test_p0_*` pattern).
- `make lint` — ruff clean.
- Manual evidence: `tests/runtime/test_install.py` proves real-loader discovery/migration/route-mount.
- CI: all four blocking profiles must pass clean (D-10); runtime suite runs in the same matrix (add to `.github/workflows/qualification.yml` only if needed to include `tests/runtime/` — `make verify` already collects all of `tests/`).
</verification>

<success_criteria>
- Real host discovery + m001 migration + route mount proven end to end (no loader mocks).
- Merchant identity, encrypted key custody, wallet binding, relays, notifications config, activation state machine, and topology refusal work over §5.1.
- Catalog CRUD with all bounds, drafts, soft-delete/tombstone intents, same-tx outbox enqueue, deterministic event builders, and dry-run route work over §5.2.
- Origin/CSRF/auth guard enforced on every admin mutation; no secret/key/token material observable in logs, responses, or DB plaintext.
- `make verify` green on SQLite (and PostgreSQL profile where run); Phase 1 evidence unaffected.
</success_criteria>

<output>
Create `.planning/phases/02-release-a-safe-web-commerce/02-01-SUMMARY.md` when done — include the resolutions of research Open Questions 1, 2, 4, 5, 7.
</output>
