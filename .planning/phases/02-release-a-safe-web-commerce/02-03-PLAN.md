---
phase: 02-release-a-safe-web-commerce
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - gammamarkets/migrations.py
  - gammamarkets/models.py
  - gammamarkets/services/fx.py
  - gammamarkets/services/orders.py
  - gammamarkets/services/checkout.py
  - gammamarkets/services/settlement.py
  - gammamarkets/services/email.py
  - gammamarkets/services/metrics.py
  - gammamarkets/services/tasks.py
  - gammamarkets/views_api.py
  - gammamarkets/views_public_api.py
  - tests/runtime/test_checkout.py
  - tests/runtime/test_invoice_saga.py
  - tests/runtime/test_settlement.py
  - tests/runtime/test_email.py
  - tests/runtime/test_order_admin.py
  - tests/runtime/test_public_contract.py
autonomous: true
requirements:
  - WEB-02
  - PAY-01
  - PAY-02
  - INV-01
  - ORD-01
  - NOTF-01
  - SEC-01

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Concurrent buyers claiming the last finite unit yield exactly one held reservation on SQLite and PostgreSQL; losers get `insufficient-stock` with no partial writes (INV-01, §8.2 step 1)."
    - "`POST /public/checkout` requires an `Idempotency-Key` `[A-Za-z0-9_-]{32,128}`; a replay returns the same order (or 409 `request-in-progress`/`idempotency-conflict`), never a duplicate order or second invoice (§4.15, WEB-02, PAY-01)."
    - "Exactly one LNbits invoice per order via `create_invoice(extension='gammamarkets', external_id='gammamarkets:{order.id}')`; the `payments` projection row (status `creating`, deterministic `core_external_id`) is inserted inside the claim transaction BEFORE the LNbits call; `InvoiceError.status=='pending'`/unknown → `creation_unknown` + `payment_exception`, reconciled by exact external_id, never blind-reissued (§8.2, §4.8, PAY-01)."
    - "A cancelled order keeps its payment projection for late-settlement detection but is never delivered a BOLT11, never reopens, and never triggers a second invoice (§8.2/§8.4, PAY-02)."
    - "Settlement is confirmed exactly once through the §8.3 transaction (listener callback AND 60s reconciliation are both idempotent); expired/late/mismatched paths follow §7.1/§8.4 without treating buyer claims or relay evidence as settlement (PAY-02)."
    - "Bearer tokens appear only as `X-Order-Token` headers — never in path/query/body, never logged, never in audit-visible surfaces; the public status payload exposes only the §5.4 field set (SEC-01, WEB-02)."
    - "Email rows dedupe per recipient via UNIQUE(order_id, channel, event_type, recipient_hash); every §8.8 enqueue point is wired (order_received, confirmed, processing, shipped, delivered, cancelled, expired, on_hold, refund_requested); only host `send_email` True enters `sent`; suppressed/failed states are explicit; no recipient PII or bearer links in subjects/logs (NOTF-01, §8.8)."
    - "Merchant order actions expose only §7.1/§7.2-legal transitions; exception resolution (accept/refund/confirm-refund) follows §8.3 with refund-as-attestation honesty (ORD-01)."
    - "Lease-protected loops (`reservation_expiry`, `reconciliation`, `retention_pruner`) acquire/renew `task_leases` with database time and a fencing token, and stop work immediately on lease loss (§10, SEC-01)."
    - "Startup reconciliation (§8.7) completes before checkout is enabled (readiness gate); every checkpoint resumes or terminates explicitly on restart (SEC-01, PAY-02)."
    - "Public rate limits enforce both checkout windows (10/min/IP and 100/hour/IP) plus the ≤100 concurrent held-reservations-per-product cap and open-order caps (§15)."
  artifacts:
    - path: "gammamarkets/migrations.py m002"
      provides: "orders, order_items, order_events, order_fx_quotes, order_fulfillment, payments, inventory_reservations, idempotency_records, email_queue + schema-only inbox_events/order_messages + §4.19 indexes"
    - path: "gammamarkets/services/fx.py"
      provides: "btc_rates adapter → Decimal conversion + provider-provenance order_fx_quotes persistence (§3.4/§16)"
    - path: "gammamarkets/services/checkout.py"
      provides: "§8.1 intake validation + server-recalculated totals, idempotency claim, §8.2 reservation CAS + creating-projection + invoice saga + public-token mint"
    - path: "gammamarkets/services/orders.py"
      provides: "§7.1 transition enforcement + order_events audit + §5.3 admin actions + per-transition email intents"
    - path: "gammamarkets/services/settlement.py"
      provides: "self-filtered invoice listener + §8.3 confirm tx + §8.4 late-payment exception + expiry + §8.7 reconciliation + reservation_expiry + retention_pruner"
    - path: "gammamarkets/services/email.py"
      provides: "§8.8 per-recipient worker on host send_email boolean boundary + opt-out + suppressed/failed states"
    - path: "gammamarkets/views_api.py + views_public_api.py"
      provides: "§5.3 admin order routes + §5.4 checkout/order-status/opt-out public routes"
    - path: "tests/runtime/test_checkout.py … test_public_contract.py"
      provides: "end-to-end web-order evidence incl. concurrency, saga races, recovery, security probes, payload-allowlist parity"
  key_links:
    - from: "gammamarkets/services/checkout.py"
      to: "lnbits.core.services.payments.create_invoice"
      via: "keyword-only call with extension/external_id correlation; InvoiceError.status drives the outcome split"
      pattern: "external_id"
    - from: "gammamarkets/services/settlement.py"
      to: "task_manager.register_invoice_listener + get_payments(external_id)"
      via: "self-filtered callback for latency + exact-external_id reconciliation for truth"
      pattern: "gammamarkets:"
    - from: "gammamarkets/services/checkout.py"
      to: "inventory_reservations + products.stock_reserved + payments"
      via: "single-transaction conditional claim (sorted-id locking) + creating projection insert before the invoice call"
      pattern: "inventory_reservations"
    - from: "gammamarkets/services/orders.py"
      to: "gammamarkets/services/email.py queue"
      via: "every admin transition/exception/expiry enqueues per-recipient intents in the same transaction"
      pattern: "email_queue"
    - from: "gammamarkets/services/email.py"
      to: "host send_email"
      via: "one call per recipient row; only True → sent"
      pattern: "send_email"
---

## Phase Goal

**As a** buyer, **I want to** check out with a server-verified total and receive exactly one invoice I can follow privately, **so that** payment truth comes from LNbits settlement alone; **as a** merchant, **I want** legal order actions and honest notifications driven by that same truth.

(Story source: REQUIREMENTS.md WEB-02/PAY-01/PAY-02/INV-01/ORD-01/NOTF-01, Phase 2 goal criteria 2–4 — backend half.)

<objective>
Build the order/payment backend of Release A: `m002` schema, the §8.1 intake + §8.2 reservation/invoice saga against the real host `create_invoice`, settlement via self-filtered listener + durable external_id reconciliation, reservation expiry, retention pruning, the §8.8 email worker with every enqueue point wired, the §5.3 admin order API and §5.4 public checkout/status/opt-out API — all through real host integration (FakeWallet settles real invoices in tests), with leases/fencing per §10.

This plan is backend-only: no HTML/JS surfaces — A2/A3 buyer UI and B1/B5 admin UI land in 02-04 on top of these APIs. The Phase 1 harness stays green (D-05).

Purpose: this is where "one authoritative inventory, no duplicate invoices, no double allocation, relay delivery never mistaken for payment truth" is proven in production code.

Output: `m002` + fx/orders/checkout/settlement/email services + task registrations + public/admin order APIs + the runtime suites proving the invariants.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-release-a-safe-web-commerce/02-CONTEXT.md
@.planning/phases/02-release-a-safe-web-commerce/02-RESEARCH.md
@.planning/phases/02-release-a-safe-web-commerce/02-UI-SPEC.md
@docs/technical-specification.md
@PINS.md

Normative spec sections this plan implements: §3.3–3.4 (order ids, money/FX snapshot), §4.7–4.9/4.12/4.15/4.18 + §4.19 (m002 tables — inbox_events/order_messages schema-only, unused until Release B), §5.3–5.4 (order admin + public checkout/status APIs), §5.6 (error codes), §7.1–7.3 (order/shipping/reservation machines), §8.1–8.4 (intake, saga, settlement, late payment), §8.7 (reconciliation), §8.8 (email worker + all enqueue points), §10 (invoice listener + reservation_expiry 30s + reconciliation startup/60s + retention_pruner daily + email_sender 5s — all leased/fenced per the §10 table), §11.3–11.4 (PII encryption, retention, public tokens), §14 (transactions, multi-worker, idempotency incl. admin-key acceptance), §15 (both checkout windows + held-reservation cap + open-order caps), §16 (metrics), §17 (failure drills), §19, §21 (decisions 3/17/21).

Binding research: `create_invoice` is keyword-only, `InvoiceError.status=="pending"` → `creation_unknown`, `external_id` is indexed-not-unique (extension-owned single-invoice enforcement via `payments` UNIQUE(order_id) + `core_external_id` correlation), listener delivery is unfiltered fan-out and non-durable (self-filter + reconcile), `update_payment_success_status` does NOT re-dispatch (reconciliation covers poll-settled payments), `bolt11.decode` for payment_hash/expiry verification, `send_email(...) -> bool` with per-recipient calls, `Connection.execute` auto-commits (all saga steps via the tx adapter / `conn.conn`), per-worker `Database` handles. The harness executable models (`harness/saga.py`, `harness/queues.py`, `harness/email.py`, `harness/state.py`, `harness/tx.py`) are the reference semantics — runtime tests re-run the key scenarios against the real host.

§8.7 note for web scope: the "type-2 outbox row" clause concerns `order_msg` rows — Release B; for web orders the operative clause is "make BOLT11 available to the token-gated status route." Do not build `order_msg` machinery.

§17 failure drills required (runtime equivalents of the P0-07/P0-08 matrix): kill between reservation and invoice call; between invoice creation and attach; between settlement and callback delivery (suppress listener, recover via reconciliation); double-deliver callback; late settlement on cancelled/expired order; multi-match external_id quarantine.

UI surfaces for these APIs land in 02-04 — keep every API response shape aligned to the UI-SPEC state/copy contract (A2/A3/B1/B5) so the UI plan needs no API rework.
</context>

<tasks>

<task type="auto">
  <name>Task 1: m002 schema, intake, reservation + invoice saga, public checkout API (WEB-02, PAY-01, INV-01)</name>
  <files>gammamarkets/migrations.py, gammamarkets/models.py, gammamarkets/services/fx.py, gammamarkets/services/orders.py, gammamarkets/services/checkout.py, gammamarkets/views_public_api.py, tests/runtime/test_checkout.py, tests/runtime/test_invoice_saga.py</files>
  <action>
1. `migrations.py` `m002_orders`: DDL per §4.7–4.9 + §4.12 + §4.15 + §4.18 — `orders` (full snapshot columns incl. address_enc/contact_enc, buyer_amount_sat, payment_exception fields, public_token_hash/enc/expires_at, email_opt_in), `order_items`, `order_events`, `order_fx_quotes`, `order_fulfillment`, `order_messages` (schema only — Release B), `payments` (UNIQUE(order_id), payment_hash, checking_id_enc, bolt11_enc, wallet_refs_enc, wallet_id_hash, source_wallet_id_hash, status creating|creation_unknown|pending|settled|expired|failed), `inbox_events` (schema only), `inventory_reservations` (UNIQUE(order_id, product_id), state held|consumed|released|expired), `idempotency_records` (scope_hash PK, in_progress|completed|failed, lease_until, response_enc), `email_queue` (UNIQUE(order_id, channel, event_type, recipient_hash), claim fields), plus every §4.19 index touching these tables. Compat helpers + `:named` params as in m001.

2. `services/fx.py`: `btc_rates(currency)` adapter — convert float→Decimal once, persist `order_fx_quotes` (rate_decimal, rate_direction, rate_unit, source, provider names, quoted_at, expires_at) per §3.4/§16; honor `allowed_currencies`; fiat totals computed server-side only.

3. `services/orders.py`: §7.1 `transition_order()` for the real schema (identical transition table to `harness/state.py` — share the literal table, not a retype) + `order_events` audit row in the same tx; §7.2 shipping and §7.3 reservation enums; admin action helpers (status transition validation, cancel-with-reason, exception resolution accept/refund/confirm-refund per §8.3, token reissue). **Every transition enqueues the matching §8.8 email intent in the same tx**: status→`processing|shipped|delivered`/`cancelled`, exception hold→`on_hold`, resolve-exception refund→`refund_requested`, settlement→`confirmed`, expiry→`expired` (the last two produced by settlement.py but enqueued through this shared helper so dedupe rules apply uniformly).

4. `services/checkout.py` — the §8.1/§8.2 pipeline: (a) `Idempotency-Key` validation + `idempotency_records` unique-insert claim before order creation (409 `request-in-progress` while leased, 409 `idempotency-conflict` on key reuse with different hash, replay returns stored response — token-bearing responses AEAD-encrypted at rest); (b) intake validation §8.1/§15 — ≤64 items, qty bounds, merchant active, products purchasable (not draft/hidden/pre-order), address required iff physical, `email_opt_in` requires `email`, shipping option valid for destination; (c) server-side recalculation: line totals from canonical product prices, shipping, FX snapshot — client-sent totals never trusted; (d) **step-1 claim transaction (single tx)**: sorted-product-id conditional UPDATE (`stock_on_hand >= qty` or unlimited) + `stock_reserved += qty` + held reservation rows with expiry + order row `received`→`invoice_pending` + **the `payments` projection insert (`status=creating`, deterministic `core_external_id=f"gammamarkets:{order.id}"`)** — the projection precedes the LNbits call so a crash before attach is visible to §8.7's `creating|creation_unknown` scan (§4.8, §8.2 step 1); (e) `create_invoice` (outside the claim tx) with `extension="gammamarkets"`, `external_id`, `expiry=RESERVATION_TTL`, memo per spec; (f) attach tx: persist hash/checking_enc/bolt11_enc/wallet snapshots whether the order is `invoice_pending` or `cancelled` — enter `awaiting_payment` + align reservation expiries only in the first case; cancelled → retain projection, never deliver BOLT11; (g) `InvoiceError.status=="failed"` → projection failed + release + reject; `"pending"`/non-InvoiceError exception → `creation_unknown` + `payment_exception` — never reissue; (h) mint the §11.4 public token (SHA-256 hash + AEAD copy while live), return once in the 201 response; (i) email intents enqueued (`order_received` merchant rows; customer rows only when opted in).

5. `views_public_api.py` additions: `POST /public/checkout` (201 shape per §5.4), `GET /public/order-status` (X-Order-Token auth — malformed/noncanonical rejected pre-lookup, `hmac.compare_digest`, dead/revoked/expired → identical "no longer valid" response with no existence oracle; response restricted to exactly the §5.4 field set: state, shipping_state, total_sat, bolt11, payment status, item summaries, expiry — no payment_hash separately, no internals), `POST /public/order-email-opt-out` (sets email_opt_in=false, cancels queued customer rows). All `no-store`/`no-referrer`; §15 limits: `CHECKOUT_RATE_LIMIT` **10/min/IP AND 100/hour/IP**, ≤100 concurrent held reservations per product, open-order caps per IP/pubkey scope — all via HMAC'd `client-ip`/`buyer-pubkey` buckets (raw IP never stored). RFC 9457 codes per §5.6.

6. `tests/runtime/test_checkout.py` (marker `runtime`): happy path 201 (order + token + total_sat + bolt11); idempotent replay returns same order; 409 in-progress while first request is mid-flight (pause fixture); key-reuse-different-body 409; validation matrix (bad items, missing address for physical, email_opt_in without email, invalid shipping destination, inactive merchant/product); server-recalculated totals override client values; both rate windows + held-reservation cap + open-order cap → `rate-limited`/`insufficient-stock` as applicable; response contains only the allowed field set.

7. `tests/runtime/test_invoice_saga.py` (marker `runtime`, both dialects — the real implementation of the P0-06/P0-07 scenarios): last-unit concurrency N buyers → exactly one held reservation, losers `insufficient-stock` cleanly; multi-item deadlock avoidance via sorted locking (PG); the `creating` projection exists inside the claim tx (kill between claim and create_invoice → §8.7-visible row); cancellation × invoice-outcome matrix (mid-saga cancel with invoice in flight) — cancelled order never reopens, never receives BOLT11, never double-releases, never double-invoices (count create_invoice calls); `creation_unknown` reconciliation attaches exactly one discovered payment by external_id; multi-match external_id → quarantined order, nothing auto-delivered.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>Web checkout creates exactly one order + one correlated invoice per idempotency scope under concurrency on both dialects; the `creating` projection exists before the LNbits call; every §15 limit enforced; the cancel×outcome matrix holds in the real implementation; tokens satisfy §11.4 end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Settlement listener, leased workers (reconciliation/expiry/retention), email worker, readiness gate (PAY-02, NOTF-01, SEC-01)</name>
  <files>gammamarkets/services/settlement.py, gammamarkets/services/email.py, gammamarkets/services/tasks.py, tests/runtime/test_settlement.py, tests/runtime/test_email.py</files>
  <action>
1. `services/settlement.py`: (a) invoice listener callback — self-filter `payment.extension=="gammamarkets"` + `external_id.startswith("gammamarkets:")`, verify wallet_id/amount/bolt11 `payment_hash`+`expiry` via `bolt11.decode`, then §8.3 confirm tx (order→confirmed, held reservations→consumed + stock_on_hand/stock_reserved decremented exactly once, order_events row, `confirmed` email intents + publication intents via orders.py helpers) — fully idempotent (already-settled payment = no-op); (b) late payment (§8.4): settlement on cancelled/expired order → payment_exception + `on_hold` intents, merchant-verified accept path only; (c) expiry: invoice expiry (decoded BOLT11 wins over RESERVATION_TTL) → expired transition + release held reservations exactly once + `expired` intents; (d) amount/wallet mismatch → payment_exception quarantine + `on_hold`, never auto-confirm.

2. Leased worker framework in `services/tasks.py` per §10: each lease-protected loop acquires/renews its `task_leases` row with database time + monotonically increasing fencing_token; loss of lease stops work immediately; every leased write carries the token; bounded batches; structured logs (ids/states only); per-unit exception isolation; cancellation-safe `finally`. Register from `gammamarkets_start`: invoice listener (`register_invoice_listener`, no lease), `email_sender` (5s, claim-fenced rows), `reservation_expiry` (30s, leased), `reconciliation` (startup + 60s, leased), `retention_pruner` (daily, leased) — joining 02-02's `outbox_publisher`/`relay_manager`; all owned handles cancelled by `gammamarkets_stop`.

3. §8.7 `reconciliation` (leased): pending payments → query core status (settled→§8.3; expired→expiry path); `creating|creation_unknown` → exact external_id lookup via `get_payments`/`PaymentFilters` (harness-proven), apply §8.2 step 5 regardless of order state; resume committed `received` orders idempotently into §8.2; reclaim stale outbox/email claims via claim-token CAS + durable evidence reconstruction; awaiting_payment web orders → ensure BOLT11 available to the token-gated status route; multi-payment external_id → quarantine. Readiness gate flips only after startup reconciliation completes — checkout returns a retry-able unavailable response before ready.

4. `reservation_expiry` (30s, leased): release expired held reservations under §8.2/§8.4 rules (exactly once, `expired` state + intents). `retention_pruner` (daily, leased): §11.3 retention — processed inbox ciphertext 7d / quarantine 30d (inert until Release B but the table exists) / order PII + message plaintext 90d post-terminal → cryptographic erasure preserving financial/audit fields; merchant-shortened retention honored; never-extended without explicit policy.

5. `services/email.py`: §8.8 worker (5s, own Database handle) — claim via claim_token CAS (dialect rules); suppressed (no SMTP call) when host email unconfigured / `GAMMAMARKETS_EMAIL_ENABLED` false / event type disabled / consent revoked / merchant inactive; render at send time from fixed templates + current order state (subject = merchant display name + event name only); per-recipient `send_email` call; only True → `sent`; False/exception → bounded unclassified retry `min(2^attempts*30s, 4h)+jitter`, 5 attempts → `failed`; magic-link rendering only while the order's token is valid + notification opted-in (AEAD copy erased on expiry/revocation); rate limits via hashed buckets (customer ≤8/h/recipient_hash, merchant ≤60/h, plus the §15 **test-send ≤5/hour** bucket keyed merchant+address); the §5.1 `POST /merchants/{id}/notifications/test` route enqueues through the same path. **Payload extension (record in summary):** `GET /merchants/{id}/notifications` gains a `queue` array (recent email_queue rows: event_type, recipient_hash-derived display hint, state, attempts, suppressed reason — never raw addresses beyond the configured list) feeding B5's per-recipient view — declared route, extended payload, no new route surface (W-NEW-1 pin).

6. §16 metrics extension: stuck-order count (orders in non-terminal states older than bounds), reservation count, email queue depth/failures — same mechanism as 02-02's metrics module.

7. `tests/runtime/test_settlement.py` (marker `runtime`, FakeWallet settles real invoices): end-to-end settle → confirmed with stock consumed exactly once + `confirmed` intents enqueued; duplicate callback = no-op; kill-between-settlement-and-callback (suppress listener, restart, reconciliation confirms exactly once); late payment on cancelled/expired → payment_exception hold + `on_hold` intents, never auto-confirm; expired invoice → expired + release once + `expired` intents; amount/wallet-mismatch → quarantine; restart mid-saga resumes from each checkpoint (received-not-reserved, reserved-not-invoiced incl. the `creating` projection path, invoiced-not-attached, claimed-not-published, claimed email rows); readiness gate blocks checkout until reconciliation completes; lease loss mid-loop stops writes (fencing token stale → 0-row write).

8. `tests/runtime/test_email.py` (marker `runtime`): every enqueue point produces rows (status/shipping/cancel transitions, on_hold, refund_requested, expired, confirmed, order_received); per-recipient dedupe (two merchant recipients → two rows; duplicate enqueue = no-op); True-only sent; False/exception → bounded unclassified retry → failed after 5; suppressed without SMTP call per reason; opt-out cancels queued customer rows + blocks new; subject/template carry no buyer PII/internal ids; token-bearing links absent after expiry/revocation; rate limits via hashed buckets; byte-level check — no plaintext recipient or raw token in the SQLite file.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>Settlement, expiry, reconciliation, retention, and email behave as the Phase-1 models proved: exactly-once confirmation, idempotent duplicate/late/mismatched handling, leased+fenced workers, restart closure at every checkpoint, readiness gating, every email enqueue point wired with honest suppressed/failed states and zero PII/token leakage.</done>
</task>

<task type="auto">
  <name>Task 3: §5.3 admin order API + backend end-to-end + public contract tests (ORD-01, SEC-01)</name>
  <files>gammamarkets/views_api.py, tests/runtime/test_order_admin.py, tests/runtime/test_public_contract.py</files>
  <action>
1. §5.3 admin order routes in `views_api.py`: `GET /orders?state=&protocol=` (+ `Filters`/`parse_filters` search across the UI-SPEC filter set incl. Expired), `GET /orders/{id}` (full detail; decrypted address for owner only), `POST /orders/{id}/status` (legal §7.1 transition only — `invalid-transition` otherwise), `POST /orders/{id}/shipping` (tracking/carrier/eta optional, tracking_enc at rest), `POST /orders/{id}/cancel` (reason required), `POST /orders/{id}/resolve-exception` (accept|refund|confirm-refund + refund_reference; refund = attestation only — response copy notes gammamarkets cannot verify outgoing payments), `POST /orders/{id}/public-token/reissue` (old token revoked immediately + AEAD copy erased, new link shown once), `GET /orders/{id}/events` (audit chronology). Owner scoping + the 02-01 security guard (incl. admin Idempotency-Key acceptance) on every mutation; each mutation enqueues its §8.8 email intents via the orders.py helper.

2. `tests/runtime/test_order_admin.py` (marker `runtime`): owner-only scoping (user B cannot read/act on user A's orders); legal-transition matrix (every legal transition accepted, every illegal rejected `invalid-transition`); cancel-with-reason releases stock once + audit row + `cancelled` intent; exception accept/refund/confirm-refund flows + attestation honesty + `refund_requested`/`on_hold` intents; token reissue revokes old immediately (old 401s, new works once); decrypted address visible only to owner; events endpoint returns chronology; admin Idempotency-Key accepted on mutations.

3. `tests/runtime/test_public_contract.py` (marker `runtime`): route table asserts no token param in any path/query; order-status response contains ONLY the §5.4 field set (explicit allowlist assertion); dead/wrong-order/nonexistent tokens → identical response (compare_digest, no oracle); no-store/no-referrer/CSP on every public response; rate limits on checkout/status/opt-out; **backend end-to-end journey**: seed catalog → publish intents → product readable via public API → checkout → FakeWallet settle → confirmed → admin sees order + audit + email rows — the Release-A loop proven at API level before the UI lands in 02-04; no secrets/tokens/full payment hashes/complete BOLT11 (admin side) in any response.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>The complete Release-A backend loop works end to end on the real host: catalog → checkout → single correlated invoice → FakeWallet settlement → confirmed → owner-scoped admin actions with legal-transition enforcement and per-transition email intents — with the token contract and restricted public payload proven at the API level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| buyer -> public checkout/status | unauthenticated + rate-limited + idempotency-scoped; token is the only capability and stays header-only |
| extension -> LNbits payments | create_invoice outcomes trusted per qualified status split; settlement truth only from host payment rows |
| listener -> settlement | unfiltered fan-out; callback must self-filter + verify before acting |
| merchant -> admin orders | owner-scoped; legal transitions only; decrypted PII to owner only |
| workers -> db | per-worker handles + task_leases fencing + claim-token CAS |
| email/logs -> third parties | recipient PII + bearer links never in subjects/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-203-01 | Tampering | duplicate invoice / double allocation | critical | mitigate | idempotency claim + UNIQUE projections + conditional stock CAS + `creating` projection in claim tx + single-invoice external_id discipline; concurrency + saga tests on both dialects |
| T-203-02 | Information disclosure | bearer token in URL/log/audit | critical | mitigate | header-only API surface (fragment handled by 02-04 page JS), redaction tests, identical dead-token responses, audit-posture gate from 02-01 |
| T-203-03 | Tampering | buyer-claimed or relay evidence as settlement | high | mitigate | settlement only via verified host payment rows |
| T-203-04 | Denial of service | stock squatting / checkout flood | high | mitigate | both checkout windows + ≤100 held/product + open-order caps via hashed buckets; reservation TTL + expiry worker |
| T-203-05 | Information disclosure | address/contact at rest or via API | high | mitigate | AEAD fields + owner-only decrypt + restricted public payload + retention pruner |
| T-203-06 | Repudiation | refund claims without proof | medium | mitigate | refund = merchant attestation recorded in order_events; copy states the limitation |
| T-203-07 | Tampering | lease-expired worker writing stale state | high | mitigate | task_leases fencing token on every leased write; lease-loss test asserts 0-row writes |
</threat_model>

<verification>
- `make verify-runtime` — checkout/saga/settlement/email/admin/contract suites green on the selected dialect.
- `make verify` — entire suite green (Phase 1 qualification + runtime); PostgreSQL dialect exercised in CI blocking matrix.
- `make lint` — ruff clean.
- Backend end-to-end journey test passes with FakeWallet settlement.
- §17 failure drills executed in the runtime suite (kill-at-checkpoint matrix).
</verification>

<success_criteria>
- Checkout backend: idempotent intake, server-recalculated totals, exactly one correlated invoice, `creating` projection before the call (WEB-02, PAY-01, INV-01).
- Payment truth: settled/expired/cancelled/late/mismatched paths follow the state machine; no blind reissue; no double allocation; restart closure holds (PAY-02, INV-01).
- Admin order API: owner-scoped legal actions, exception resolution, token reissue, audit chronology (ORD-01).
- Notifications backend: every §8.8 enqueue point wired, per-recipient honest states, opt-out (NOTF-01).
- Security: token custody, PII encryption/retention, all §15 limits, leased workers, log redaction (SEC-01).
</success_criteria>

<output>
Create `.planning/phases/02-release-a-safe-web-commerce/02-03-SUMMARY.md` when done.
</output>
