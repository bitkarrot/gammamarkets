---
phase: 02-release-a-safe-web-commerce
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - gammamarkets/__init__.py
  - gammamarkets/services/transport.py
  - gammamarkets/services/relay.py
  - gammamarkets/services/outbox.py
  - gammamarkets/services/tasks.py
  - gammamarkets/services/nip89.py
  - gammamarkets/services/themes.py
  - gammamarkets/services/metrics.py
  - gammamarkets/services/merchant.py
  - gammamarkets/models.py
  - gammamarkets/views.py
  - gammamarkets/views_api.py
  - gammamarkets/views_public_api.py
  - gammamarkets/templates/gammamarkets/public_base.html
  - gammamarkets/templates/gammamarkets/public_product.html
  - gammamarkets/templates/gammamarkets/public_collection.html
  - gammamarkets/templates/gammamarkets/public_merchant.html
  - gammamarkets/static/gammamarkets/js/public_storefront.js
  - gammamarkets/static/gammamarkets/css/gm-public.css
  - gammamarkets/static/gammamarkets/css/themes/warm-market.css
  - gammamarkets/static/gammamarkets/css/themes/clean-minimal.css
  - gammamarkets/static/gammamarkets/css/themes/high-contrast.css
  - tests/runtime/test_outbox.py
  - tests/runtime/test_nip89.py
  - tests/runtime/test_public_pages.py
  - tests/runtime/test_themes.py
autonomous: true
requirements:
  - PUB-01
  - WEB-01
  - UI-01
  - UI-03
  - SEC-01

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Outbox rows are claimed with dialect-correct fencing (PG `FOR UPDATE SKIP LOCKED`, SQLite `BEGIN IMMEDIATE` bounded select/update), every leased write compares `claim_token`, and stale claims are reclaimed only via CAS + durable `relay_publications` reconstruction (§8.6, §14, PUB-01)."
    - "Every publish attempt records one `relay_publications` row per copy/relay with the verbatim `SendEventOutput` success/failed split; accepted copy/relay targets are never resent, and relay ACKs are never treated as payment or business truth (§8.6, PUB-01)."
    - "Publication ordering follows §8.6 (30406→30405→30402, dependencies honored, supersession on newer revision, deletion republishes survivors before the kind-5 tombstone); `created_at=max(db_now, latest_created_at+1)` with clock-skew pause rather than backdating (PUB-01)."
    - "The extension owns its nostr-sdk `Client` lifecycle: created with bounded connection parameters per §9.5, used by the publisher, closed in `gammamarkets_stop` alongside owned task handles — never attached to a raw merchant nsec (§10, §11.2)."
    - "Relay targets come only from validated `relay_configs` (full §9.5 set enforced on write in 02-01 and revalidated on connect); embedded naddr relay hints are parsed but never fetched (§5.4, §9.5)."
    - "`GET /gammamarkets/p/{naddr}` resolves only local kind-30402 coordinates for a local merchant with a valid d, never fetches embedded relay hints, and renders or redirects to the canonical product page (§5.4, WEB-01)."
    - "Public pages are standalone documents with `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, restrictive CSP without third-party scripts, and HTTPS-only lazy images — they never extend `base.html` (§5.4, 02-UI-SPEC §A)."
    - "Merchant theme choices persist as a validated allowlisted token object (preset/brand/advanced), apply only under `.gm-public` on public documents, and the WCAG ≥4.5:1 save gates block invalid pairs server-side (02-UI-SPEC, UI-03)."
    - "The merchant layout preset (Editorial/Guided/Compact) renders on public pages and never overrides the compact mobile fallback — ≤560px always wins (UI-01)."
  artifacts:
    - path: "gammamarkets/services/transport.py"
      provides: "owned nostr-sdk Client lifecycle — bounded §9.5 connection params, connect/publish/close, registered for stop-hook cleanup"
    - path: "gammamarkets/services/outbox.py"
      provides: "§8.6 publisher worker: fenced claims, dependency gating, build-from-current-state, per-relay evidence, outcome/backoff policy, stale-claim reconstruction"
    - path: "gammamarkets/services/relay.py"
      provides: "relay_configs target resolution + health aggregation over relay_publications + connection-state surface for B2"
    - path: "gammamarkets/services/tasks.py"
      provides: "task registry + gammamarkets_start wiring: outbox_publisher 5s + relay_manager-lite health tick + readiness gate structure"
    - path: "gammamarkets/services/nip89.py"
      provides: "naddr parse/local-resolution + canonical-redirect logic for /p/{naddr}"
    - path: "gammamarkets/services/themes.py"
      provides: "theme token object validation, WCAG contrast save gates, preset registry, .gm-public emission"
    - path: "gammamarkets/views.py + views_public_api.py"
      provides: "public page routes (/p/{naddr}, /p/{pubkey}/{d}, collections, merchant profile) + §5.4 read-only public JSON"
    - path: "gammamarkets/static/gammamarkets/css/gm-public.css + themes/*.css"
      provides: "`.gm-public`-scoped token architecture + three preset stylesheets from the sketch theme sources"
    - path: "tests/runtime/test_outbox.py,test_nip89.py,test_public_pages.py,test_themes.py"
      provides: "transport, handler, header/CSP, and theme-gate evidence"
  key_links:
    - from: "gammamarkets/services/outbox.py"
      to: "gammamarkets/services/events.py + keystore.py + transport.py"
      via: "claims pending rows, builds unsigned events from current state, signs via key store, sends via owned Client.send_event_to"
      pattern: "send_event_to"
    - from: "gammamarkets/services/outbox.py"
      to: "outbox_events/relay_publications tables"
      via: "claim-token CAS leased writes + append-only per-relay evidence rows"
      pattern: "claim_token"
    - from: "gammamarkets/views.py public routes"
      to: "gammamarkets/services/themes.py + gm-public.css"
      via: "standalone documents emitting .gm-public-scoped tokens with restrictive CSP"
      pattern: "gm-public"
    - from: "gammamarkets/services/relay.py"
      to: "relay_publications + transport client state"
      via: "health aggregation + live connection state feeding relay-health API"
      pattern: "relay_publications"
---

## Phase Goal

**As a** merchant, **I want** my catalog to publish to relays with durable per-relay evidence and my storefront to render under my chosen bounded theme, **so that** buyers can discover products through Nostr/NIP-89 while I retain evidence of delivery.

(Story source: REQUIREMENTS.md PUB-01/WEB-01/UI-01/UI-03, Phase 2 goal criteria 1–2 — transport + presentation half. Publication-health *display* lands with the admin UI in 02-04.)

<objective>
Turn the 02-01 domain into a live public storefront and working publication pipeline: the extension-owned nostr-sdk transport with bounded connections, the durable outbox publisher with per-relay ACK evidence and §8.6 ordering, the NIP-89 naddr handler, the standalone themed public documents (product, collection, merchant profile), the §5.4 read-only public JSON, and the theme-token backend with WCAG save gates — per the approved `02-UI-SPEC.md` and sketch findings.

This plan deliberately carries no admin UI and no checkout — B2–B6 admin surfaces and A2/A3 land in 02-04 after the order backend (02-03) exists. The Phase 1 harness stays green (D-05).

Purpose: the outbox is the highest-risk async component (fencing, per-relay evidence, supersession, ordering); landing it with real `LocalRelay` integration before checkout depends on it keeps the vertical slice honest.

Output: `services/transport.py|outbox.py|relay.py|nip89.py|themes.py|tasks.py`, public templates/static, and runtime tests covering transport, headers, themes, and publication evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-release-a-safe-web-commerce/02-CONTEXT.md
@.planning/phases/02-release-a-safe-web-commerce/02-RESEARCH.md
@.planning/phases/02-release-a-safe-web-commerce/02-UI-SPEC.md
@.devin/skills/sketch-findings-gammamarkets/SKILL.md
@.planning/sketches/MANIFEST.md
@docs/technical-specification.md
@PINS.md

Normative spec sections this plan implements: §5.4 (public read routes + document security headers), §6.1–6.5 + §6.7–6.8 (publication content via 02-01 builders), §7.4 (outbox states), §8.6 (publish algorithm), §9.1/9.5 (transport interface, relay URL/egress rules incl. revalidate-on-connect), §10 (outbox_publisher + relay-manager scope + stop-hook client cleanup), §11.2 (signing via keystore), §15 (public read rate limits), §16 (metrics + observability of publication evidence), §19 (relay/egress threats), §21 decisions (clock-skew posture, deactivation tombstones).

§10 `relay_manager` scope for Release A: subscriptions/inbox-relay restore are Release B; Release A ships a minimal transport manager — one owned `Client`, bounded connection params (§9.5: connection count, handshake time, frame size, idle time, per-host concurrency), connect-on-start for configured public relays, health tick (30s) feeding `relay_configs` connection state, closed in `gammamarkets_stop`. Record the Release-B scoping rationale in the summary.

UI contract (binding): `02-UI-SPEC.md` — tokens (4 sizes 14/16/24/36, 2 weights, spacing scale, 60/30/10 color, semantic tokens never merchant-editable), copywriting contract (literal strings), breakpoints (lg ≥1024, md 561–1023, sm ≤560 — compact always wins), `.gm-public` scoping, WCAG save gates. Surfaces implemented here: A1 (public product/collection/merchant) and the theme/preset *rendering* half of UI-01/UI-03. Admin surfaces B2–B6 and A2/A3 are 02-04 scope — do not build them here.

Preset token sources: `.devin/skills/sketch-findings-gammamarkets/sources/themes/{default,clean-minimal,high-contrast}.css` (Warm Market default). Sketches under `.planning/sketches/` are the visual guideline.

Research bindings: `Client.send_event_to(urls, event) -> SendEventOutput(success, failed)` is the per-relay evidence source; resolve OQ6 (partial-connectivity semantics — timeout vs failed-map entry, connect precondition) with an early `LocalRelay` + dead-URL probe and record the finding. `static_url_for` cache-busts extension assets via the `gammamarkets_static_files` host mount. Public pages MUST NOT extend `base.html`. Host `installed-extension` middleware 404s `/gammamarkets/*` on deactivation — storefront offline is expected; the merchant deactivation flow (§6.7 tombstone-first) is the graceful path.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owned transport client, outbox publisher, relay health (PUB-01, §9.5, §10)</name>
  <files>gammamarkets/__init__.py, gammamarkets/services/transport.py, gammamarkets/services/relay.py, gammamarkets/services/outbox.py, gammamarkets/services/tasks.py, gammamarkets/services/metrics.py, gammamarkets/views_api.py, tests/runtime/test_outbox.py</files>
  <action>
1. OQ6 probe first (in `test_outbox.py`, against `harness/relay.py` `LocalRelay` + a dead URL): pin `send_event_to` semantics — connect precondition, unreachable-relay behavior (exception vs failed-map entry vs timeout), success/failed split shape — and record findings in the summary. The publisher must handle every observed behavior (wrap exceptions into per-relay `timeout`/`rejected` evidence rows, never crash the claim batch on one relay).

2. `services/transport.py`: extension-owned `nostr_sdk.Client` lifecycle — created with bounded §9.5 connection parameters (bounded connection count, handshake/frame/idle bounds, per-host concurrency), connected to the validated public relay set at start, shared by the publisher, closed inside `gammamarkets_stop` (§10 — SDK clients closed; every handle has cancellation-safe `finally`). No raw nsec ever attaches to the client — signing happens per event through `keystore.sign_event`. Relay targets revalidate through the shared §9.5 validator on connect/reconnect.

3. `services/relay.py`: target resolution (public write set from `relay_configs`, honoring direction/enabled), connection-state surface (from the owned client + health tick), and health aggregation over `relay_publications` (per-relay last accepted/rejected/timeout + pending counts) consumed by `GET /merchants/{id}/relay-health` (wire the 02-01 stub to real data). **Spec-surface delta (record in summary):** B2's per-row outbox view needs two admin routes beyond the §5.1 table — declare `GET /merchants/{id}/outbox` (outbox_events rows + per-relay relay_publications outcomes + dependency/supersession markers, owner-scoped, paginated ≤100) and `POST /merchants/{id}/outbox/{intent_id}/retry` (requeue a `failed|partially_published` intent: reset attempts/next_attempt_at — never resend accepted targets). Both behind the same admin guard; document the delta in the plan summary (W-NEW-1 pin).

4. `services/outbox.py`: the §8.6 worker — (a) claim ≤OUTBOX_BATCH rows `pending|partially_published` with `next_attempt_at<=db_now` and `attempts<20`, dialect-correct (PG `FOR UPDATE SKIP LOCKED` in one tx; SQLite `BEGIN IMMEDIATE` + bounded select/update), setting claimed_by/claimed_until/state=claimed and incrementing claim_token; (b) skip rows with unpublished `outbox_dependencies`; supersede a row when a newer revision exists for its supersedable aggregate (rebind dependents per §8.6 step 2); (c) build unsigned events from CURRENT domain state via `services/events.py`, sign via `keystore.sign_event`, `created_at=max(db_now, latest_created_at+1)` with clock-skew pause + health signal instead of backdating; (d) `Client.send_event_to` the public relay set via the owned transport; persist one `relay_publications` row per relay with verbatim result+truncated message; (e) outcome policy — published at quorum (≥1 positive OK for public events), partially_published retrying only missing targets, zero-OK → pending with `min(2^attempts*5s, 30min)+jitter`, ≥20 attempts → failed; accepted targets never resent; (f) every leased write compares claim_token; lease-expired stale claims reconstruct durable accepted targets from relay_publications before requeue. Per-worker `Database` handle (Pitfall 4).

5. `services/tasks.py`: worker registry — register from `gammamarkets_start`: `outbox_publisher` (interval=5) and `relay_manager` (interval=30 health tick — connect/reconnect the owned transport to the configured set, update connection state; Release-B subscription duties stay deferred and documented). **§10 lease deviation (record in summary):** the Release-A relay_manager tick runs unleased per worker — each worker owns its own `Client` and manages only its own connections, so per-worker ticks are correct on multi-worker PostgreSQL topology; it writes no shared lease-fenced rows (connection state is per-process, publication evidence writes stay claim-fenced in the publisher). All handles owned + cancelled by `gammamarkets_stop`. Readiness-gate structure established (reconciliation completes it in 02-03); public catalog reads may serve, checkout stays gated.

6. §16 metrics: emit structured metrics hooks from the workers — outbox depth + oldest-pending age, per-merchant relay health counts, failed-publication count — as a `services/metrics.py`-style module (or a small section inside tasks.py) exposing the counters the admin health surfaces and logs consume. No secrets/PII in emitted fields.

7. `tests/runtime/test_outbox.py` (marker `runtime`, both dialects): OQ6 probe results; claim fencing (two workers, no double-claim; stale token write affects 0 rows); dependency ordering (30406→30405→30402 sequence against LocalRelay); per-relay evidence verbatim incl. dead-relay failure; accepted-target never resent across retries; partial→missing-only retry; zero-OK backoff bounds; 20-attempt → failed; supersession on newer revision; deletion ordering (survivors republished before kind-5 tombstone); clock-skew pause path; restart mid-claim reconstructs from durable publications (CAS only); `gammamarkets_stop` closes the client + cancels owned tasks and nothing else.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make lint</automated>
  </verify>
  <done>The durable outbox publishes real signed events to real relays (LocalRelay) through an owned, bounded, cleanly-stopped client — verbatim per-relay evidence, fenced claims, §8.6 ordering/supersession/backoff, restart reconstruction, and §16 metrics. OQ6 finding recorded.</done>
</task>

<task type="auto">
  <name>Task 2: NIP-89 handler + public read API + standalone storefront pages (WEB-01)</name>
  <files>gammamarkets/services/nip89.py, gammamarkets/views.py, gammamarkets/views_public_api.py, gammamarkets/templates/gammamarkets/public_base.html, gammamarkets/templates/gammamarkets/public_product.html, gammamarkets/templates/gammamarkets/public_collection.html, gammamarkets/templates/gammamarkets/public_merchant.html, gammamarkets/static/gammamarkets/js/public_storefront.js, tests/runtime/test_nip89.py, tests/runtime/test_public_pages.py</files>
  <action>
1. `services/nip89.py` + `views.py`: `GET /gammamarkets/p/{naddr}` — `Nip19Coordinate.from_bech32`, require kind 30402 + local merchant pubkey + valid d_tag, ignore embedded relay hints entirely (no server-side fetch), render or 301-redirect to canonical `/gammamarkets/p/{merchant_pubkey}/{d_tag}`; malformed/wrong-kind/foreign → the UI-SPEC invalid-link response ("This product link is not valid here.").

2. `public_base.html` + public templates: standalone documents (own `<html>` — never `base.html`), emitting `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, restrictive CSP (`default-src 'self'`; no third-party scripts; `img-src https:`), HTTPS-only lazy-loaded merchant images with `referrerpolicy="no-referrer"`. Only the vendor assets actually needed (vue/quasar UMD from `/static/vendor/`; axios optional).

3. A1 product page `GET /gammamarkets/p/{merchant_pubkey}/{d_tag}` per UI-SPEC §A1: `.product-layout` grid, gallery ≤16, title/price/currency/availability, sanitized markdown description (allowlist sanitizer server-side + purify belt client-side), specs, shipping options, variation selector (invalid combination → inline error, no submit). All states: available / sold ("Sold out" chip, buy controls replaced) / hidden ("This product is not available.") / pre-order ("Pre-order — purchasing opens later.") / not-found / inactive merchant. Layout preset honored (Editorial/Guided/Compact) with compact fallback forced ≤560px. The buy affordance renders the product form (fields + totals preview per the UI-SPEC invariants) with submission pointed at the 02-03 checkout API — the API route may not exist yet in this plan's test window, so gate the submit control on endpoint availability and keep the page fully honest (no dead buttons; "Checkout is being enabled" style fallback is unnecessary — prefer wiring the real POST and letting 02-03 complete the loop; if the route is absent at test time, the form presents but submit returns the API error surface honestly).

4. Collection browse `GET /public/collections/{merchant_pubkey}/{d_tag}` + merchant profile `GET /public/merchants/{pubkey}` pages per §5.4 + UI-SPEC copy states (zero-member collections never render; long titles truncate; 64KB descriptions scroll in the sanitized region). The `/gammamarkets/order` document shell can land now as an empty frame (02-04 fills A3 behavior) or defer entirely to 02-04 — choose one and keep it honest.

5. `views_public_api.py`: read-only public JSON per §5.4 — `GET /public/products/{merchant_pubkey}/{d_tag}`, `GET /public/collections/{merchant_pubkey}/{d_tag}`, `GET /public/shipping/{merchant_pubkey}/{d_tag}`, `GET /public/merchants/{pubkey}` — no internals, `no-store`/`no-referrer`, database-backed rate limits via `rate_limit_buckets` enforcing the §15 bound **120 GET requests/minute/IP** (HMAC'd `client-ip` scope, never raw IP). Where a path serves both HTML (A1/collection/merchant pages) and JSON, dispatch on the `Accept` header — JSON contract fields are never influenced by HTML needs.

6. `tests/runtime/test_nip89.py` (marker `runtime`): valid local naddr → canonical page/redirect; malformed bech32, wrong kind (30405 naddr), foreign merchant pubkey, unknown d_tag → invalid-link response with no relay fetch (assert zero outbound relay connect attempts — fixture records them); relay hints present-but-ignored; rate-limit enforcement on public routes.

7. `tests/runtime/test_public_pages.py` (marker `runtime`): each public route returns the required headers (no-store, no-referrer, CSP sans third-party); product states render correct copy (sold/hidden/pre-order/not-found); preset switching changes only presentation; ≤560px renders compact structure; no merchant internals (nsec, tokens, internal ids) in any public payload/HTML; public JSON exposes only the §5.4 field set.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make lint</automated>
  </verify>
  <done>Buyers can browse a standalone, CSP-restricted storefront (product/collection/merchant/naddr) with all documented states and the adaptive layout contract; the NIP-89 handler resolves local naddrs without network fetches; public JSON serves under required headers and rate limits.</done>
</task>

<task type="auto">
  <name>Task 3: Theme token backend + preset pipeline + public scoping (UI-01, UI-03)</name>
  <files>gammamarkets/services/themes.py, gammamarkets/services/merchant.py, gammamarkets/models.py, gammamarkets/views_api.py, gammamarkets/static/gammamarkets/css/gm-public.css, gammamarkets/static/gammamarkets/css/themes/warm-market.css, gammamarkets/static/gammamarkets/css/themes/clean-minimal.css, gammamarkets/static/gammamarkets/css/themes/high-contrast.css, tests/runtime/test_themes.py</files>
  <action>
1. `services/themes.py`: theme token object model — `preset` (warm-market|clean-minimal|high-contrast), `brand` (display name, ≤3-char logo initials, primary accent, preset font-stack choice, corner character→radius set), `advanced` (bounded token list only per UI-SPEC: `--color-bg`, `--color-surface`, `--color-surface-alt`, `--color-border`, `--color-text`, `--color-text-muted`, `--color-primary`, `--color-on-primary`, `--color-primary-hover`, `--color-accent`, `--radius-*`, `--space-*` — semantic/focus keys rejected, no free-form CSS/scripts/URLs/fonts). Persistence via `settings` table or a merchant column (pick per §4.17; record choice). Layout-preset field (editorial|guided|compact) lives on the same object — UI-01.

2. WCAG save gates server-side: compute contrast for `--color-text` vs `--color-bg`/`--color-surface`, `--color-primary` vs `--color-on-primary`, focus token validity — block writes failing ≥4.5:1 with RFC 9457 detail naming the failing pair + computed ratio. The admin editor UI (02-04) reuses the same computation client-side for live preview; server remains authoritative.

3. Emission: public templates render the resolved token object as CSS custom properties scoped under `.gm-public` (Task 2 templates consume this); `gm-public.css` holds shared token architecture + structural rules; preset stylesheets ported from `.devin/skills/sketch-findings-gammamarkets/sources/themes/` expressed in the UI-SPEC token names. Theme never reaches admin documents (no stylesheet/token object on admin responses — assert in tests).

4. Theme read endpoint for public rendering is internal (template-time resolution, no public theme API needed). Admin theme read/write rides the declared §5.1 routes: `GET /merchants/current` gains a `theme` object in its response payload, and `PATCH /merchants/{id}` accepts a `theme` field — no new route surface (W-NEW-1 pin).

5. `tests/runtime/test_themes.py` (marker `runtime`): token-object allowlist enforcement (unknown key/semantic-key/free-form CSS rejected); contrast gate math (failing pairs blocked with named pair+ratio; passing pairs saved); Advanced Tokens requires the opt-in flag; Brand Basics bounds (≤3-char initials, preset font stacks only, radius mapping); reset-to-preset clears overrides; `.gm-public` scoping present on public docs and absent everywhere else; layout-preset value honored in A1 render while ≤560px still forces compact.
  </action>
  <verify>
    <automated>make host && make verify-runtime && make verify && make lint</automated>
  </verify>
  <done>The three presets + bounded Brand Basics + guarded Advanced Tokens persist as validated token objects, WCAG gates block invalid saves server-side, public pages render merchant themes under `.gm-public` only, and the layout preset renders with the compact mobile fallback intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public routes -> internet | unauthenticated, rate-limited, read-only catalog data; naddr handler performs no outbound fetch |
| extension -> relays | `send_event_to` results are evidence, not truth; relay URLs restricted to the validated configured set (§9.5) |
| theme tokens -> public documents | validated allowlist only; never admin surfaces; contrast gates enforced server-side |
| worker -> db claims | fenced claim-token CAS; stale workers cannot commit |
| owned Client -> process lifecycle | created with bounded params, closed on stop; raw keys never attach |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-202-01 | Information disclosure | public pages leaking internals | high | mitigate | standalone docs + restricted JSON + tests scanning for secrets/ids |
| T-202-02 | Tampering | forged publication state | high | mitigate | claim_token CAS on every leased write; durable relay_publications reconstruction |
| T-202-03 | Repudiation | lost/misattributed relay evidence | medium | mitigate | verbatim SendEventOutput persisted per copy/relay |
| T-202-04 | Denial of service | relay timeouts stalling claims | medium | mitigate | bounded batches, per-relay failure isolation (OQ6 probe), backoff bounds, bounded client params |
| T-202-05 | Elevation | theme token injection (CSS/script) | medium | mitigate | allowlisted token object, no free-form CSS, CSP on public docs |
| T-202-06 | Information disclosure | naddr relay hints used for SSRF | medium | mitigate | hints parsed but never fetched; local-only resolution asserted by test |
| T-202-07 | Tampering | relay URL validated once then redirected | medium | mitigate | §9.5 revalidation on connect/reconnect, not just at write time |
</threat_model>

<verification>
- `make verify-runtime` — outbox/nip89/public/theme suites green on the selected dialect; PostgreSQL exercised in CI (claim SQL differs per dialect — both variants tested).
- `make verify` — full suite green including Phase 1.
- `make lint` — ruff clean.
- LocalRelay integration is the transport evidence — no external relays required.
</verification>

<success_criteria>
- Signed catalog/merchant events reach relays through the durable outbox with verbatim per-relay evidence, correct ordering, supersession, backoff, and restart reconstruction (PUB-01).
- Local NIP-89 handler + canonical product/collection/merchant pages serve under the public security headers with all documented states (WEB-01).
- Editorial/Guided/Compact presets render with compact mobile fallback; theme presets + Brand Basics + guarded Advanced Tokens persist behind WCAG gates with public/admin separation (UI-01, UI-03).
- §16 metrics hooks emit outbox/relay-health/reservation-adjacent counters for the admin surfaces 02-04 renders.
</success_criteria>

<output>
Create `.planning/phases/02-release-a-safe-web-commerce/02-02-SUMMARY.md` when done — include the OQ6 (send_event_to partial-connectivity) finding and the Release-B scoping rationale for full relay_manager duties.
</output>
