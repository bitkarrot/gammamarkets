---
phase: "2"
slug: "release-a-safe-web-commerce"
status: draft
shadcn_initialized: false
preset: none
created: "2026-09-20"
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for Release A — Safe Web Commerce. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Authority order:** `docs/technical-specification.md` is normative on every payment, security, state-machine, routing, and privacy question. `.devin/skills/sketch-findings-gammamarkets/` (sketch winners: 001-D Adaptive Blend, 002-A Linear Split, 003-D Tiered Controls) is binding on presentation and interaction. This contract never overrides either.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (no shadcn — project is not React/Next.js/Vite; extension runs inside the LNbits host shell) |
| Preset | not applicable |
| Component library | Quasar 2.22.0, host-vendored UMD (`Quasar` global) + Vue 3.5.34 global build (`Vue` global), provided by the pinned LNbits `v1.6.2-rc1`/`e336fe1` base template. No npm build step, no React, no Tailwind, no shadcn — sketch decision, locked |
| Icon library | Material Icons (Quasar default icon set, host-vendored) |
| Font | Admin: LNbits host system stack (inherit, do not override). Public: `--font-sans` per preset — Warm Market `ui-rounded, "Avenir Next", system-ui, sans-serif`; Clean Minimal & High Contrast `Inter, system-ui, sans-serif`. Stacks are local/system fallbacks only — **no remote font loading, ever** (spec §15/§19) |
| Monospace | `"SFMono-Regular", Consolas, monospace` — invoice strings, token fragments, technical evidence |

**Delivery model:** extension pages are server-rendered Jinja templates mounted into the host shell (`{% block vue_templates %}` on `#vue`) for admin, plus standalone extension templates for public buyer pages. Public pages share the same token architecture but are NOT mounted inside the admin shell — they are independent documents with a restrictive CSP and no third-party scripts (spec §5.4).

---

## Component Inventory

> Quasar is the project's design system, supplied by the host — there is no installed npm package in this repository to query. Inventory is enumerated from the host-vendored bundle actually served to browsers.

Enumerated by `grep -o 'Q[A-Z][A-Za-z]*' .cache/lnbits/lnbits/static/vendor/quasar.umd.prod.js | sort -u` — ~126 Q-component exports — `quasar@2.22.0` (pinned host `package.json`; vendored UMD bundle) — 2026-09-20.

Non-exhaustive list of known-good components for this phase. Checking the Quasar 2.22 docs for anything outside this table is the expected path, not an exception.

| Component | Import path | Notes |
|-----------|-------------|-------|
| QBtn / QBtnGroup | `Quasar` global | All actions; `unelevated` primary, `outline` secondary, `flat` tertiary. Every state-changing action carries a text label — no icon-only mutators |
| QCard / QCardSection / QCardActions | `Quasar` global | Checkout card, admin panels, metrics |
| QInput / QSelect / QCheckbox / QOptionGroup | `Quasar` global | Form fields; inline `bottom-slots` error text, never toast-only validation |
| QField | `Quasar` global | Read-only wrapped values (invoice string, order ref) |
| QTable / QTd / QTr | `Quasar` global | Outbox table, catalog lists; dense mode for admin |
| QList / QItem / QItemSection / QItemLabel | `Quasar` global | Order list rows, nav |
| QBadge / QChip | `Quasar` global | State pills — styled via semantic tokens, never merchant theme |
| QBanner | `Quasar` global | Exception banner, topology/top-level warnings, tombstone disclosure |
| QDialog | `Quasar` global | Destructive confirmations (cancel-with-reason, delete, deactivate, token reissue) |
| QExpansionItem | `Quasar` global | Progressive disclosure: technical delivery evidence, advanced shipping fields |
| QStepper / QStep / QStepperNavigation | `Quasar` global | Guided checkout preset stepper (Product → Delivery → Pay) |
| QTabs / QTab / QTabPanels / QTabPanel | `Quasar` global | Admin sub-navigation within a surface (never a wide top-tab strip copied from nostrmarket) |
| QTimeline / QTimelineEntry | `Quasar` global | Order chronology inside the detail pane |
| QSkeleton / QSpinnerDots / QInnerLoading | `Quasar` global | Loading states; spinner is never the sole payment feedback |
| QTooltip | `Quasar` global | Hints on disabled/illegal actions — explain *why* an action is unavailable |
| QSeparator / QSpace / QScrollArea / QInfiniteScroll / QPagination | `Quasar` global | Layout and long-list mechanics |
| QForm | `Quasar` global | Validation wrapper on admin CRUD and checkout forms |
| vue-qrcode | host-vendored `vue-qrcode` global | Invoice QR — host provides the component; do not bundle a second QR lib |
| purify (DOMPurify) | host-vendored `purify.js` | Client-side belt on top of the mandatory server-side markdown allowlist sanitizer (spec §15) |

---

## Spacing Scale

Declared values (multiples of 4 only — mapped to the preset token names from `.devin/skills/sketch-findings-gammamarkets/sources/themes/*.css`):

| Token | Value | Usage |
|-------|-------|-------|
| `--space-1` (xs) | 4px | Icon gaps, pill padding, inline meta |
| `--space-2` (sm) | 8px | Compact element spacing, row inner gaps |
| `--space-3` | 12px | Field padding, filter bar gaps |
| `--space-4` (md) | 16px | Default element spacing, card padding |
| `--space-6` (lg) | 24px | Section padding, panel gaps |
| `--space-8` (xl) | 32px | Layout gaps, workspace gutters |
| `--space-12` (2xl) | 48px | Major section breaks, empty-state padding |
| `--space-16` (3xl) | 64px | Page-level editorial spacing (public surfaces only) |

Exceptions: **44px minimum interactive target** on all public touch controls (sketch 001 decision) and 40px minimum on dense admin rows/inputs; 360px fixed summary/rail columns are layout widths, not spacing.

---

## Typography

Exactly four roles and two weights. Sizes map to preset `--text-*` tokens; public presets may scale the Display/Heading tokens ±1 step per preset character, but Body and Label are invariant everywhere (checkout readability is a save-gated invariant).

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px (`--text-base`) | 400 | 1.5 |
| Label | 14px (`--text-sm`); 12px (`--text-xs`) for meta/timestamps only | 400 / 600 | 1.4 |
| Heading | 20px (`--text-xl`) / 24px (`--text-2xl`) | 600 | 1.2 |
| Display | 30px (`--text-3xl`) / 36px (`--text-4xl`) — public surfaces only | 600 | 1.2 |

Rules: 400 regular and 600 semibold are the only two weights — status pills and totals use 600, not the sketch's 760. Monospace stack is reserved for BOLT11 fragments, tokens, and technical evidence. Admin surfaces inherit host typography and never receive theme font tokens.

---

## Color

### 60/30/10 Contract (public surfaces, expressed as theme tokens)

| Role | Token | Warm Market (default) | Clean Minimal | High Contrast | Usage |
|------|-------|----------------------|---------------|---------------|-------|
| Dominant (60%) | `--color-bg` | `#f7f1e8` | `#f4f7f7` | `#0b0f14` | Page background |
| Secondary (30%) | `--color-surface` / `--color-surface-alt` | `#fffaf3` / `#f1e4d3` | `#ffffff` / `#e9efef` | `#141a21` / `#1d2630` | Cards, panels, nav, wells |
| Accent (10%) | `--color-primary` + `--color-on-primary` | `#a34f2a` / `#ffffff` | `#266760` / `#ffffff` | `#ffb000` / `#111111` | Reserved elements below |
| Destructive | `--color-danger` | `#b33a3a` | `#a62f39` | `#ff6b6b` | Destructive actions + exception states only |

Accent (`--color-primary`) reserved for: primary CTA buttons, links, active/selected indicators (order-row left rule, progress-step active, focus outlines where `--color-focus` delegates), and the total's emphasis. `--color-accent` (Warm `#d69a3a` / HC `#63d2ff`) is a secondary highlight for preview banners and decorative flourishes — never on actionable controls.

### Semantic colors — system-controlled, never merchant-editable

`--color-success` `#24705a`/`#1f7255`/`#59e3a7`, `--color-warning` `#b87319`/`#a56613`/`#ffd166`, `--color-info` `#356b8c`/`#286f91`/`#63d2ff`, `--color-danger` (above), `--color-focus` `#225bdb`/`#165dff`/`#ffffff`. Advanced Tokens exposes background/surface/text/border/radius/spacing — semantic and focus tokens are excluded from every tier.

### Dark / light handling

There is no runtime dark-mode toggle. High Contrast is the dark preset (dark bg, light text, near-black `--color-on-primary`). Admin surfaces always render in the host LNbits palette — public theme choice cannot darken or recolor admin.

### WCAG save gates (blocking, spec sketch-003 locked)

1. `--color-text` vs `--color-bg` and vs `--color-surface`: ≥ 4.5:1 — block save on failure, show the failing pair and computed ratio.
2. `--color-primary` vs `--color-on-primary`: ≥ 4.5:1 — block save.
3. `--color-focus` must remain a visible, valid token — block save.
4. Semantic colors are not editable; any payload attempting to set them is rejected.
5. Live preview recomputes contrast on every edit, before save — color controls without live contrast feedback are forbidden.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (pre-invoice) | "Review payment" (Guided/Compact step CTA "Continue to delivery" precedes it) |
| Primary CTA (invoice state) | "Copy invoice" — secondary action; the QR + truncated BOLT11 is the primary surface. Status polling is automatic |
| Order-status page CTA | "Copy status link" and "Stop order emails" (opt-out) |
| Merchant order actions | State-machine-named verbs: "Start processing", "Mark shipped", "Mark delivered", "Cancel with reason", "Accept paid order", "Request refund", "Confirm refund sent", "Resolve exception", "Reissue status link" |
| Empty state heading (orders) | "No orders yet" |
| Empty state body (orders) | "New web and protocol orders will appear here." |
| Empty-filter body | "No matching orders — adjust search or status filters." |
| Empty catalog | "No products yet — create a product or save a draft to begin." |
| Error state (list load) | "Orders could not be loaded. The durable queue remains safe — retry without changing order state." + "Retry" |
| Checkout validation | Inline, field-level, `aria-live="polite"`: "Enter a valid email for order updates." / "Choose a shipping option for this destination." |
| Invoice expired | "Invoice expired — no payment was taken. Inventory will be released safely; create a new invoice only after status reconciliation finishes." + "Review order again" |
| Payment uncertain | "Payment status is being verified with the payment provider. No new invoice has been created — do not pay a second invoice." (never auto-reissue, spec §8.2) |
| Cancel order | "Cancel order NRG-1042": "Cancelling releases reserved stock and does not refund a settled payment. A reason is required and is recorded in the audit log." + reason field + "Cancel order" |
| Delete product | "Delete {title}": "This is a soft delete. A deletion tombstone is published, but removal from every relay and client cannot be guaranteed." (spec §6.7) + reference report or explicit strip-and-republish choice when still referenced |
| Reissue status link | "Reissue status link": "The old link stops working immediately. Share the new link only with the buyer." |
| Merchant deactivation | Two-step: "Begin deactivation" lists blocking nonterminal orders/exceptions first; step 2 requires tombstone ACKs or explicit override |
| nsec import | "Import Nostr secret key" — masked input, "Sent once over TLS and never displayed or logged." Request-body logging disabled (spec §5.1) |
| Refund confirm | "Confirm refund sent": "Records your attestation that a refund was paid from the wallet UI. gammamarkets cannot verify outgoing payments." (decision 3) |

**Copy rules (hard constraints):**

- Never render secrets, nsec, key material, bearer tokens, full payment hashes, or complete BOLT11 in admin UI text. Buyer invoice view shows the full BOLT11 (it is the buyer's own payment request); admin technical evidence truncates to `payment_hash` correlation only (spec §16, sketch-002).
- Settlement language is authoritative: "Payment confirmed" appears only after LNbits settlement. Relay ACKs are delivery evidence, never "paid"; a kind-17 receipt is "buyer-claimed" at most (`receipt_verified` is a cosmetic badge).
- Email subjects carry merchant display name + event name only — never buyer PII or internal order ids (spec §8.8).
- Error copy = problem + what happens next. No raw stack traces, codes, or SQL.
- NIP-04 surface (Release C only) must be labeled "legacy / metadata-exposed" — noted here as a boundary so Release A components don't preempt it.
- Remote product images carry a standing disclosure in admin: "Remote image hosts can learn viewer IP and time" (spec §15).
- RFC 9457 `urn:gammamarkets:*` error codes map to friendly copy: `insufficient-stock` → "Not enough stock available — reduce quantity or choose another item."; `order-expired` → the expired copy above; `rate-limited` → "Too many requests — wait a minute and try again."; `invalid-shipping-destination` → "This item cannot be shipped to the selected destination."

---

## UI Considerations

Applicable state considerations resolved: 24 covered, 2 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty | order list, catalog, collections, outbox, relay health, notify_emails | ✅ covered | Documented empty copy per surface above; empty outbox shows "No publications pending" (healthy, not an error) |
| loading | order list, product page, order-status poll, publication health | ✅ covered | QSkeleton rows for lists; order-status shows last-known state + "checking…" indicator; spinner never sole payment feedback |
| error | list load, checkout submit, naddr resolve, token lookup | ✅ covered | Error copy contract above; naddr malformed/wrong-kind/foreign renders a plain "This product link is not valid here." — no relay fetch, no detail (spec §5.4) |
| pending | invoice `awaiting_payment`, outbox `pending/claimed/publishing`, merchant `publication_pending` | ✅ covered | Countdown pill + waiting copy; outbox rows show per-relay pending state; merchant activation shows "Publishing…" with per-relay progress |
| expired | invoice expiry, reservation TTL, public_token 30-day expiry | ✅ covered | Expired copy + "Review order again"; dead token shows "This order link is no longer valid." without confirming order existence |
| partial-failure | outbox `partially_published`, per-relay ACK split, email `suppressed/failed` | ✅ covered | Per-relay outcome cells (ACKed/pending/failed) with retry counts; email row shows suppressed reason (SMTP unconfigured / opted out / disabled) vs failed-after-5-attempts |
| exception | `payment_exception` on order, `creation_unknown`, late-settled-after-cancel, mismatched settlement, oversold accept | ✅ covered | Danger banner atop detail pane naming the exception + only legal actions (accept / refund / confirm-refund); `oversold=true` shows backorder disclosure incl. `backordered_qty` |
| zero-one-many | order list, collections (must contain ≥1 active product to publish), relay list | ✅ covered | One order opens detail directly on mobile; collection editor blocks publish at zero active members (spec §6.2) |
| long-text | product titles/descriptions (200/500/64 KB bounds), buyer IDs, naddr strings | ✅ covered | Titles truncate with ellipsis in list rows; descriptions render sanitized markdown in scrollable region; npub/token strings truncate middle with copy affordance |
| overflow | order list (100s), timeline (long audit), outbox table | ✅ covered | QInfiniteScroll/pagination on list; timeline scrolls inside detail pane; outbox table paginated |
| sold-out | product `available=0` / `nip99_status=sold` | ✅ covered | "Sold out" chip replaces buy controls; checkout submit still rejects server-side (`insufficient-stock`) |
| hidden/draft | `visibility=hidden`, `draft=true` | ✅ covered | Hidden excluded from web catalog (direct URL renders "This product is not available."); drafts show a "Draft — never published" badge in admin |
| stock-squatting | per-IP/pubkey open-order caps | 🧪 backstop | Cap-hit renders `rate-limited` copy; held-reservation exhaustion is a disclosed DoS-inventory risk (spec §15) — visual drill is a held-out check |
| reduced-motion / focus | all public surfaces | 🧪 backstop | `prefers-reduced-motion` kills transitions; `:focus-visible` always visible via `--color-focus` — verified visually at UAT |

---

## Surfaces and Component Contracts

### A. Public buyer surfaces (themed, independent documents)

All public pages are standalone HTML documents: `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, restrictive CSP with no third-party scripts, `img-src https:`. Merchant images render HTTPS-only, `referrerpolicy="no-referrer"`, lazy-loaded (spec §5.4/§15).

**A1 — Product page** `GET /gammamarkets/p/{merchant_pubkey}/{d_tag}` and `GET /gammamarkets/p/{naddr}` (NIP-89 handler; resolves locally only, renders or redirects to the canonical page — malformed/wrong-kind/foreign naddr → "This product link is not valid here.")

- Layout (Editorial preset, desktop): `.product-layout` grid `minmax(0,1.15fr) minmax(360px,0.85fr)`, gap `clamp(28px,5vw,72px)` — gallery + context left, persistent checkout card right.
- Content: gallery (≤16 images), title, price + currency, availability (`stock` = on_hand − reserved; unlimited shows no count), sanitized markdown description, specs, shipping options, variation selector (variation children of a `variable` parent; invalid combination → inline error, no submit).
- States: available / sold (`nip99_status=sold` or available=0 → "Sold out" chip, buy controls replaced) / hidden (excluded from catalog; direct URL → "This product is not available.") / pre-order (visible, discovery-only — not purchasable in v1, decision 14 → "Pre-order — purchasing opens later.") / not-found / inactive merchant.
- Collection page `GET /public/collections/{pubkey}/{d}` and catalog browse reuse the same card grid; zero-member collections are never publishable so never rendered.

**A2 — Adaptive checkout** `POST /public/checkout` + invoice view (sketch 001-D Adaptive Blend — LOCKED)

- Merchant selects ONE preset — Editorial (rich display + embedded checkout card), Guided (QStepper: 1 Product → 2 Delivery → 3 Pay), Compact (progressive-disclosure single column). Buyers never see a layout chooser.
- **Invariants across all presets (cannot be overridden by layout or theme):** persistent order summary — Items / Shipping / **Total** always visible before invoice creation; identical field set (country select, address iff any item is physical, optional email, `email_opt_in` checkbox "Send transactional updates. No marketing."); identical validation (inline, `aria-live`); identical payment states and security copy.
- Field bounds (spec §15): ≤64 items, qty ≤10,000 (and merchant cap); address required iff physical; `email_opt_in` requires `email` → consent-without-email error "Enter an email to receive order updates, or turn updates off."
- Idempotency: client generates `Idempotency-Key` `[A-Za-z0-9_-]{32,128}` ≥128 random bits once per checkout session and retries with the SAME key; a 409 replay renders the existing order, not a duplicate.
- Invoice state: QR (host vue-qrcode), copyable BOLT11, integer sat amount, decoded-BOLT11 expiry countdown (BOLT11 wins over `RESERVATION_TTL`), "Waiting for payment" status pill, polling `GET /public/order-status` every 5s with `X-Order-Token`. Copy: "Invoice is correlated to this order only. Creating it does not mark the order paid."
- Invoice-state machine on screen: `creating` (skeleton, no QR yet) → `awaiting_payment` (QR + countdown) → `confirmed` (success view + order ref + chronology) | `expired` (expired copy) | `creation_unknown` (payment-uncertain copy — NEVER a second invoice) | `cancelled`.
- No "pay again" affordance after uncertainty; the only recovery is the reconciled status poll or a merchant-issued new checkout.

**A3 — Order status page** `GET /gammamarkets/order` (spec §5.4/§11.4 — token contract is normative)

- Shareable link: `/gammamarkets/order#<token>`. On load, JS reads the fragment, immediately `history.replaceState`-removes it, holds the token in memory only, and sends it as `X-Order-Token` on every 5s poll until terminal state. Token NEVER appears in path or query (spec decision 17).
- Renders ONLY: state, shipping_state, total_sat, bolt11, payment status, item summaries, expiry — plus an email opt-out action (`POST /public/order-email-opt-out`). Never merchant internals, internal ids, buyer address/contact echoes, tracking numbers, or other orders' data.
- Buyer-facing state labels: `awaiting_payment` → "Waiting for payment" + invoice block; `confirmed` → "Payment confirmed"; `processing` → "Preparing your order"; `completed` → "Complete"; `cancelled`/`rejected`/`expired` → "This order is no longer active" (+ merchant-visible reason only when public-safe); `payment_exception` → "On hold — the merchant is reviewing a payment issue."
- Dead/invalid/revoked/expired token → "This order link is no longer valid." — identical copy for nonexistent orders (no existence oracle).

### B. Merchant admin surfaces (host-styled, LNbits-consistent — NEVER merchant-themed)

Admin pages mount inside the host shell; density, nav, status colors, and action hierarchy follow LNbits conventions. One sidebar nav section: Orders / Catalog / Publications / Settings (merchant, notifications, appearance).

**B1 — Order workspace** (sketch 002-A Linear Split — LOCKED; UI-02, ORD-01)

- `.workspace` grid `390px minmax(0,1fr)`: searchable/filterable list left, persistent detail right. Selection survives filter/status changes.
- Filters row: search input ("Search order or buyer") + state select (All / Awaiting payment / Confirmed / Needs attention / Cancelled). Count summary in header ("N active orders · M need attention").
- Order row: reference, channel chip (Web/Gamma/NIP-15), buyer handle (web = email as supplied; protocol = truncated npub), item summary, total in sats, state pill. Active row: `inset 3px 0 var(--color-primary)` left rule + 10% tint.
- Detail pane order (fixed): ① heading (ref, channel, buyer, state pill) → ② exception banner (danger, names the exception + explains) → ③ legal contextual actions ONLY (derived from §7.1/§7.2 — illegal transitions are hidden or disabled with a tooltip reason, never offered) → ④ metrics grid (Total / Payment / Inventory / Protocol) → ⑤ items + fulfillment (address shown decrypted to owner only) → ⑥ chronological `order_events` timeline → ⑦ collapsed "Technical delivery details" (truncated payment_hash correlation, relay ACK evidence — never keys, full BOLT11, or bearer tokens).
- Legal action map: `confirmed` → Start processing / Cancel with reason (reason required, refund warning — "never implies money was returned"); `processing` → Mark shipped (tracking/carrier/eta optional, tracking encrypted) → Mark delivered; `awaiting_payment`/`invoice_pending` → Cancel; `payment_exception` → Accept paid order (oversell/backorder flow disclosed) / Request refund / Confirm refund sent; `expired|cancelled` with settled payment → accept via exception only; terminal → "No legal action" disabled.
- Shipping exception: `exception` reachable from `processing|shipped`; recovery action requires reason, recorded in `order_events`.
- Token rotation: "Reissue status link" (web orders) — revokes old token immediately, shows new link once.
- Mobile (≤560px): list → full detail surface on selection with explicit "← Back to orders".

**B2 — Publication health dashboard** (PUB-02)

- Per-relay table: relay URL, connection state, last positive/negative ACK, pending counts. Relay ACKs labeled "delivery evidence" — never "published = paid" conflation.
- Outbox table (QTable, dense): event kind (0/30402/30405/30406/31989/31990), aggregate ref, state pill (`pending|claimed|publishing|partially_published|failed|superseded`), attempts, per-relay outcome cells, supersession marker. `order_msg` rows never show superseded (spec §7.4) — out of Release-A scope but the cell renderer must not lie.
- States: empty outbox ("No publications pending"), partial (yellow — names the non-ACKed relays), failed (danger + retry affordance), clock-skew publication pause surfaced as a health failure (decision 8).
- Payment exceptions queue surfaced as its own attention list.

**B3 — Catalog management** (CAT-01/02)

- Entities: catalogs, products (simple/variable/variation × digital/physical), collections, shipping options. Each row: title, d_tag, type/format, price, stock (on_hand − reserved; `NULL` → "Unlimited"), visibility pill (on-sale / hidden / pre-order), draft badge, published state.
- Product editor: identity (title ≤200, summary ≤500, description ≤64 KB markdown with allowlist sanitizer), price `(amount_minor, currency, decimals)`, images (≤16, HTTPS-only URLs, no server-side fetch + viewer-IP disclosure), specs, categories, weight/dims, stock (NULL = unlimited), visibility, draft toggle ("Draft — never published in v1"), collections membership (published products MUST belong to ≥1 published collection — publish blocked at zero, spec §6.1), shipping option references + `extra-cost`.
- Variation editor: exactly one `a` parent link; a variation cannot be a parent (depth=1 enforced in UI).
- Shipping editor: title, base price, currency, service (standard/express/overnight/pickup — pickup requires location or geohash), countries (non-empty ISO alpha-2), regions, duration min≤max with H|D|W unit, carrier, weight/dim bounds; `price-distance` is rejected in v1 (spec §6.3).
- Deletion: always soft-delete dialog; when still referenced → reference report OR explicit strip-and-republish (merchant chooses, spec §6.7); removing a collection's last active product offers tombstone-or-replacement in the same flow. Copy carries the no-guarantee disclosure.
- `GET /products/{id}/events` dry-run preview: rendered unsigned event JSON per protocol in a read-only viewer before publish.

**B4 — Merchant settings & identity** (MERC-01)

- Identity card: display name, pubkey (hex, copyable — public key is not secret), profile fields, `payment_preference` fixed "manual" badge (decision 7), `SPEC_REVISION` shown (§12).
- Key actions: generate OR import nsec (masked input, TLS-only note, never echoed/logged); key material never rendered after entry. Wallet binding: LNbits wallet selector, ownership revalidated on write; change blocked while any order is `invoice_pending|awaiting_payment` — disabled control carries that reason.
- Relay config: write/discovery relay list (wss:// normalized), NIP-89 handler pair status.
- Activation state machine `draft → publication_pending → active → deactivating → inactive`: "Publish everything" enqueues republication; "Begin deactivation" runs the two-step flow listing blocking nonterminal orders/exceptions, tombstones commerce + NIP-89 events but NOT kind-0/10050 by default (explicit key-retirement checkbox, spec §6.7). UI must not claim NIP-17 reachability until kind-10050 ACK — Release-A shows the Gamma-orders channel as "Not available in this release".
- Unsupported topology: SQLite multi-process/CockroachDB → blocking QBanner "This database topology is not supported for production" (spec §14, decision 15).

**B5 — Notification settings** (NOTF-01)

- `notify_emails`: ≤5 validated merchant addresses; per-event toggle set (default `order_received, confirmed, on_hold`); "Send test" per address (≤5/hour).
- Customer email explainer: strictly per-order opt-in, transactional only, link is a bearer token (disclosed). Queue view surfaces `sent / pending / suppressed (reason) / failed after N attempts` — never claims transient-vs-5xx distinction (decision 21).

**B6 — Appearance settings** (sketch 003-D Tiered Controls — LOCKED; UI-03)

- Three progressive tiers in one editor + live public preview:
  1. **Preset** (default): Warm Market / Clean Minimal / High Contrast radio cards with swatch strip.
  2. **Brand Basics** (opt-in): store display name, logo initials (≤3 chars, no image upload in v1), primary accent color, approved type style (preset font stacks only), corner character (sharp/rounded/soft → `--radius-*` set).
  3. **Advanced Tokens** (explicit opt-in gate — checkbox + warning): bounded token list only — `--color-bg`, `--color-surface`, `--color-surface-alt`, `--color-border`, `--color-text`, `--color-text-muted`, `--color-primary`, `--color-on-primary`, `--color-primary-hover`, `--color-accent`, `--radius-*`, `--space-*`. No arbitrary CSS, scripts, markup, URLs, or fonts — persisted as a validated allowlisted token object, never free-form CSS.
- Layout preset selector lives here too: Editorial / Guided / Compact cards (UI-01) with the note "Mobile always uses the compact layout — responsive safety overrides this preference."
- Every tier: live preview of PUBLIC surfaces only, contrast meter per gated pair, save blocked on gate failure with the failing pair + computed ratio, and one-click "Reset to preset" (every tier reversible).
- Persistent boundary note in the editor: "Appearance applies to your public storefront only. It never changes the admin area, checkout fields, totals, validation, or payment states."

---

## Responsive Breakpoints

| Token | Width | Behavior |
|-------|-------|----------|
| `lg` | ≥1024px | Full layouts: editorial product grid, 390px+detail workspace, two-column settings grid |
| `md` | 561–1023px | Admin sidebar collapses to 72px icons; workspace list narrows to 320px; detail-grid → 1fr; settings-grid → 1fr; product grid → 1fr (sketch 001/002/003 breakpoints ~820–860px unified at 1024 for the contract; implementations may collapse at ≤860px if earlier stacking reads better — measured on real content) |
| `sm` | ≤560px | Compact mobile fallback — **always wins over merchant layout preference**: single-column checkout, list→detail navigation with "← Back to orders", filters stack, sidebar hidden, header actions collapse into overflow menu |

Responsive fallback is a safety rule, not a suggestion: a merchant's Editorial/Guided choice never produces a desktop-squeezed mobile checkout (sketch-001 avoid list). All public controls keep ≥44px targets at every width.

---

## Public / Admin Theme Separation Boundary

- Theme tokens are emitted as CSS custom properties scoped under a `.gm-public` root on public documents only. Admin documents never receive the theme stylesheet or the token object.
- No code path may apply merchant theme values to admin surfaces, checkout field order/validation, totals visibility, payment states, focus visibility, or security copy — these are invariants enforced in review and by the contrast/save gates.
- Theme settings persist as a validated token object (`preset`, `brand`, `advanced` — all allowlisted keys); free-form CSS is never stored or rendered (sketch-003).
- Admin uses the host palette and the semantic tokens above for state pills; public uses preset/brand tokens with the same semantic state semantics (success/warning/danger/info meaning is stable across presets).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — project has no shadcn/npm frontend |
| third-party | none declared | not applicable — all components are host-vendored Quasar/Vue globals |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS
- [ ] Dimension 7 Inventory Provenance: PASS

**Approval:** pending
