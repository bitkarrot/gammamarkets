---
phase: 01-conformance-profile-contract-phase-0
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - harness/sdk.py
  - harness/relay.py
  - harness/nip89.py
  - harness/authprobe.py
  - harness/fx.py
  - harness/registry.py
  - harness/evidence.py
  - tests/qualification/test_p0_04_relay_ack.py
  - tests/qualification/test_p0_05_encrypted_fixtures.py
  - tests/qualification/test_p0_11_nip89_route.py
  - tests/qualification/test_p0_12_auth_privacy_lifecycle.py
  - tests/qualification/test_p0_13_decimal_fx.py
  - tests/qualification/test_p0_14_contract_closure.py
  - tests/fixtures/golden/nip17/.gitkeep
  - tests/fixtures/golden/nip89/.gitkeep
  - tests/fixtures/golden/nip15/.gitkeep
  - PINS.md
autonomous: true
requirements:
  - QUAL-04
  - QUAL-05
  - QUAL-11
  - QUAL-12
  - QUAL-13
  - QUAL-14

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Local accepting, rejecting, and silent relays produce positive OK, negative OK, and timeout, and no event is ever sent to an unlisted relay (QUAL-04/P0-04)."
    - "Fixed kind-16 recipient and sender copies preserve rumor id across retry, use independent wrappers, route only to the respective party's relays, and reject every tampered outer/seal/rumor chain without plaintext logging (QUAL-05/P0-05)."
    - "A valid local kind-30402 naddr resolves locally; malformed, wrong-kind, and foreign references fail without fetching relay hints (QUAL-11/P0-11)."
    - "ID-only and cross-origin cookie mutations fail, approved bearer/CSRF paths pass, audit capture redacts secrets, and startup readiness and cancellation-safe task/SDK cleanup satisfy the qualified host profile (QUAL-12/P0-12)."
    - "Decimal units, provider provenance, five-minute freshness, per-component ceilings, and rejection-before-reservation hold for FX quotes, and the float-boundary error is measured and recorded (QUAL-13/P0-13)."
    - "Every normative transition, field, route, event fixture, release gate, and gammamarkets identifier closes with no undeclared dependency, and P0-01..P0-14 all have recorded evidence (QUAL-14/P0-14)."
  artifacts:
    - path: "harness/sdk.py"
      provides: "NIP-59 wrap/unwrap builders and rumor construction through nostr-sdk only"
    - path: "harness/nip89.py"
      provides: "naddr decode, kind/pubkey/d validation, and local-only resolution model"
    - path: "harness/authprobe.py"
      provides: "probe router with bearer/CSRF/origin enforcement, audit redaction model, readiness gate, and cancellation-safe cleanup probe"
    - path: "harness/fx.py"
      provides: "§3.4 FxQuote model: Decimal units, provenance, 5-minute freshness, ROUND_CEILING per component, checked sums, float-boundary measurement"
    - path: "harness/registry.py"
      provides: "P0-14 closure registries: states, transitions, schema fields, routes, event kinds, error codes, task names, settings, release gates, identifiers"
    - path: "tests/fixtures/golden/nip17/"
      provides: "checked-in golden fixtures for kind-16 rumor, seal, and gift-wrap chains with tamper matrix"
    - path: "tests/fixtures/golden/nip89/"
      provides: "checked-in golden fixtures for the 31989/31990 pair and a valid 30402 naddr"
    - path: "tests/fixtures/golden/nip15/"
      provides: "checked-in literal NIP-15 30017/30018 DTO fixtures"
    - path: "tests/qualification/test_p0_14_contract_closure.py"
      provides: "P0-14 contract-closure gate against spec + registries + schema + fixtures + evidence"
  key_links:
    - from: "tests/qualification/test_p0_05_encrypted_fixtures.py"
      to: "harness/relay.py + harness/sdk.py"
      via: "wrap/unwrap round-trips published to party-scoped local relays with tamper matrix"
      pattern: "rumor"
    - from: "tests/qualification/test_p0_14_contract_closure.py"
      to: "docs/technical-specification.md + harness/registry.py + harness/schema.py + harness/state.py + tests/fixtures/golden/"
      via: "registry parity against spec extraction, schema/state coverage, fixture coverage, release scoping, and evidence P0 map completeness"
      pattern: "gammamarkets"
    - from: "harness/fx.py"
      to: "harness/schema.py"
      via: "invalid quotes must reject before any reservation row exists (order_fx_quotes + inventory_reservations)"
      pattern: "ROUND_CEILING"
---

## Phase Goal

**As a** merchant-operator, **I want to** prove the exact LNbits/SDK/platform/database profile with reproducible qualification evidence before any production runtime is built, **so that** merchants' real funds are never exposed to an unqualified contract.

(Story source: REQUIREMENTS.md user story "As an operator, I can prove the exact LNbits/SDK/deployment profile before merchants use real funds", adapted to the Phase 1 qualification-harness boundary.)

<objective>
Build the protocol, relay, auth/privacy, FX, and contract-closure fixtures that complete the conformance profile (workstream 01-03 from the ROADMAP: "Build protocol, relay, auth/privacy, FX, and contract-closure fixtures"): relay ACK classification and NIP-17 wrap validation/routing fixtures with golden + seeded generated cases (P0-04, P0-05), NIP-89 local routing and host auth/privacy/lifecycle probes (P0-11, P0-12), the Decimal/FX boundary model with measured float-boundary error (P0-13), and the contract-closure gate proving no undeclared state, field, route, identifier, or release dependency remains and that P0-01..P0-14 all carry recorded evidence (P0-14).

Purpose: Release A must not ship on unproven relay-ACK semantics, unproven wrap validation, a broken NIP-89 entry point, assumed host auth behavior, or drifting FX math — and the owner cannot approve the pins (D-11) without a closed evidence bundle.

Output: extended harness modules (sdk, relay, nip89, authprobe, fx, registry, evidence report rendering), six p0-named test modules, checked-in golden fixtures for NIP-17/NIP-89/NIP-15 plus a kind-30402 naddr, seeded generated edge cases, PINS.md completion (FX measured bound, protocol namespace pin, approval section), and the final evidence bundle.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-conformance-profile-contract-phase-0/01-CONTEXT.md
@.planning/phases/01-conformance-profile-contract-phase-0/01-01-SUMMARY.md
@.planning/phases/01-conformance-profile-contract-phase-0/01-02-SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@docs/technical-specification.md

Normative spec sections this plan implements: §3.4 (FX boundary and measured float error), §5.4 (naddr handler rules), §5.6 (error codes), §6.5 (NIP-89 pair, 31989 d=30402), §6.6 (literal NIP-15 DTOs), §6.8 (NIP-32 namespace pinned in PINS.md), §6.9 (order messages/rumors, two-copy rule), §7.1-7.5 (state enums for the closure registry), §8.6 (relay outcome policy), §8.8 (email boundary — log redaction), §9.1 (deterministic local relays authoritative, NostrTransport interface), §9.3 (party-relay routing), §10 (task names, readiness, cancellation-safe cleanup), §12 (settings), §14 (topology refusal), §17 (golden fixture and security test requirements), §18 (release gates A/B/C), §21 decisions 6/17/21/23/25/26/27, §22 P0-04/P0-05/P0-11/P0-12/P0-13/P0-14.

Plans 01-01 and 01-02 conventions are binding: markers already registered in tests/conftest.py (do not modify it — put fixtures in harness/ modules or test-local code), dialect switch via LNBITS_DATABASE_URL, evidence plugin derives P0 ids from module names, harness/relay.py and harness/sdk.py exist from plan 01-01, and harness/schema.py + harness/state.py from plan 01-02 are the closure gate's comparison targets. Verify nostr-sdk 0.44.8 API signatures (wrap/unwrap, targeted publish, per-relay results) against the installed package before coding — never invent signatures.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relay ACK classification + NIP-17 encrypted fixtures with golden and seeded generated cases (P0-04, P0-05; QUAL-04, QUAL-05)</name>
  <reversibility rating="costly">The checked-in golden fixtures become normative cross-phase references: later releases rerun against them and external comparisons rely on them, so changing them after Phase 1 requires re-qualification rather than an edit.</reversibility>
  <files>harness/sdk.py, harness/relay.py, tests/qualification/test_p0_04_relay_ack.py, tests/qualification/test_p0_05_encrypted_fixtures.py, tests/fixtures/golden/nip17/.gitkeep, tests/fixtures/golden/nip89/.gitkeep</files>
  <action>
1. harness/sdk.py (extend): NIP-59 builders through nostr-sdk only (no custom crypto, STACK.md non-selection): construct an unsigned rumor, seal it (kind 13, empty tags) and gift-wrap it (kind 1059) with a fresh ephemeral wrapper key per copy, per §6.9 — seal and wrap timestamps independently randomized up to two days in the past, rumor unsigned, exactly one p tag on the wrap. Add an unwrap/verify path: outer signature/id verification, NIP-44 decrypt outer -> seal, verify seal signature and id and empty tags, decrypt seal -> rumor, require rumor id to equal the canonical NIP-01 hash, rumor unsigned, and rumor.pubkey == seal.pubkey (the outer key is never identity). Verify the installed SDK's NIP-44/NIP-59 API names before coding and record what you found in the SUMMARY.

2. tests/qualification/test_p0_04_relay_ack.py (marker protocol): using harness/relay.py from plan 01-01, publish one signed synthetic event through the SDK to an accepting relay, a rejecting relay, and a silent relay, and assert the SDK surfaces positive OK, negative OK (with the relay's message), and timeout respectively — WebSocket send success alone is never delivery evidence (§8.6). Then assert targeted routing: publishing with an explicit target list never sends to an unlisted relay — start a recording relay, exclude it from the target list, publish, and assert it received zero events. If the installed SDK cannot return per-relay acceptance results or cannot target an explicit relay subset, STOP and record a blocking qualification failure under D-13 (this is exactly the nostrclient fan-out deficiency the research identified — the SDK must expose better). Add the optional external smoke as an env-gated skipped-by-default test (GAMMA_QUAL_SMOKE_RELAY=wss://nostr.net enables it): ephemeral throwaway key, non-sensitive synthetic event, result recorded as advisory and never a substitute for the deterministic local evidence (§21.27).

3. tests/fixtures/golden/nip17/: checked-in golden fixtures (D-08) generated once with the SDK from fixed test keys: a kind-16 type-1 order rumor (common tags: exactly one p, one subject, one type, one order; plus amount and item tags per §6.9), its seal, and two independently wrapped copies (buyer/recipient and merchant/sender) with distinct outer event ids and wrapper keys; a retry pair showing the same canonical rumor id with fresh seals/wrappers/outer ids. Also golden fixtures under tests/fixtures/golden/nip89/ for the 31989/31990 pair (31989 d is the literal 30402; 31990 d is the recommended-app id — the pair's distinct d values must be caught by fixtures per §6.5) and one valid bech32 kind-30402 naddr string. Fixtures carry no real keys — synthetic test keys only. Record the generation script/seed in the fixture directory README so regeneration is deterministic.

4. tests/qualification/test_p0_05_encrypted_fixtures.py (marker protocol): (a) round-trip: each golden wrap unwraps to the exact golden rumor with the full §8.5 verification chain passing; (b) tamper matrix: every tampered chain is rejected — tampered outer content, tampered outer signature, tampered seal content/signature, rumor/seal pubkey mismatch, rumor id mismatch, signed rumor, duplicate common tags, wrong rumor kind — each rejection happens without plaintext in any error path; (c) rumor identity across retry: the retry pair preserves the rumor id while outer ids/keys/timestamps differ, and receivers dedupe retry wraps by rumor id; (d) party routing: publish the recipient copy only to the buyer-relay fixture set and the sender copy only to the merchant-relay fixture set, assert each relay received exactly its copy and no other; (e) seeded generated edge cases (D-08): from the golden fixtures, generate deterministic mutated variants with a fixed-seed RNG (seed recorded; default seed constant) covering oversized values, missing tags, duplicate tags, and bit-flipped fields, and assert every generated case is rejected by the validation chain; (f) no plaintext logging: capture logs across all wrap/unwrap/reject paths and assert the decrypted rumor content and conversation keys never appear (PITFALLS NIP-17 privacy leak).
  </action>
  <verify>
    <automated>make host && make verify-protocol</automated>
  </verify>
  <done>P0-04 and P0-05 evidence passes: positive OK, negative OK, and timeout classified from deterministic local relays with zero leakage to unlisted targets; golden fixtures prove two-copy construction, retry identity, party-only routing, and full tamper-chain rejection without plaintext logging; seeded generated cases all reject — all recorded under P0-04/P0-05 in the evidence manifest, with golden fixtures checked in.</done>
</task>

<task type="auto">
  <name>Task 2: NIP-89 local routing + host auth/privacy/lifecycle probes + literal NIP-15 DTOs (P0-11, P0-12; QUAL-11, QUAL-12)</name>
  <files>harness/nip89.py, harness/authprobe.py, tests/qualification/test_p0_11_nip89_route.py, tests/qualification/test_p0_12_auth_privacy_lifecycle.py, tests/fixtures/golden/nip15/.gitkeep</files>
  <action>
1. harness/nip89.py: local naddr resolution model per §5.4: bech32-decode the naddr (prefer the installed SDK's NIP-19 API if it exposes bech32/naddr decoding — verify first; otherwise vendor the bech32 decoder pattern from the nostrclient reference checkout with attribution, it is encoding not cryptography), then require kind 30402, a merchant pubkey present in the local registry, and a valid d identifier (alphabet/length per §3.1), and resolve to the local canonical product page — never fetching embedded relay hints, never contacting relays (PITFALLS: broken NIP-89 link). Malformed bech32, wrong kind, foreign merchant pubkey, and invalid d each fail with a distinct rejection. The local registry is fixture data mapping (merchant pubkey, d) to a local path.

2. tests/qualification/test_p0_11_nip89_route.py (marker protocol): valid golden naddr resolves locally to the canonical page; malformed, wrong-kind (e.g. a 30018 naddr), and foreign-merchant references fail with clear rejections; relay hints never trigger a fetch — embed a hint pointing at the SILENT-mode local relay from plan 01-01 in the naddr fixture and assert the relay received zero connections and zero requests during resolution. Golden naddr and NIP-89 pair fixtures come from Task 1's fixture directories.

3. tests/fixtures/golden/nip15/: literal NIP-15 DTO fixtures per §6.6 — a 30017 stall JSON (id, name, description, currency, shipping array with id/name/cost/regions) and 30018 product JSON (id, stall_id, name, description, images, currency, price, quantity as integer or null, specs as pair arrays, shipping array) — plus invalid variants: specs as object instead of pair-array, quantity as float or string, missing shipping id, opaque-address physical order shape, mismatched currencies between stall and product, hidden/pre-order mapped to quantity 0, and the deterministic zero-cost digital zone rule. A validator (in the test module or a small helper in harness/nip89.py's directory sibling — keep it in the test module unless reused) asserts the literal DTO shapes and the §6.6 lossy rules: these DTO checks are the release-scoped compatibility restriction evidence (§18 Release C gate planned now, not blocking Release A).

4. harness/authprobe.py + tests/qualification/test_p0_12_auth_privacy_lifecycle.py (marker host): probe the qualified host's auth/privacy/lifecycle boundaries with a minimal probe router mounted only inside the host app fixture from plan 01-01 (no production routes): (a) ID-only and cross-origin mutations fail — a state-changing probe route requires an authenticated session AND a same-origin check the route itself enforces (host CORS is never trusted, §21.29): cookie-only without origin, and cookie with a cross-origin Origin header, are both rejected; the approved cookie+origin path passes; (b) approved bearer path — the public-status probe route requires the bearer token via header, rejects path/query token carriage, and sets Referrer-Policy no-referrer with Cache-Control no-store (§5.4 rules exercised at probe level); (c) audit redaction — a probe audit logger records a synthetic event containing a bearer token, a bolt11 string, and a recipient address, and the persisted audit rows contain only redacted markers and identifiers (never secrets, PII, or invoice material; §16); (d) startup readiness — a probe start function performs only bounded registration through task_manager.create_permanent_task (no inline network/reconciliation work) and a readiness gate stays closed until a startup pass completes (§10); assert checkout stays gated before readiness; (e) cancellation-safe cleanup — register a probe task and an SDK client connected to a local relay, then cancel the task directly (simulating process shutdown without the stop hook) and assert the finally-cleanup closed the relay connection (the relay fixture records the disconnect); also exercise the stop-hook path cancelling only extension-owned handles (never the host's cancel-everything API, §10); (f) unsupported-topology refusal — the probe asserts a production-ready mode flag refuses to enable for an unsupported topology (multi-process SQLite) per §14 (model-level check of the refusal rule); (g) request-logging redaction — the probe router logs every request it serves to a structured request log (method, route, status, non-secret correlation identifiers); drive the approved bearer path from (b) carrying a valid X-Order-Token, capture the router's request-logging surface, and assert the token value never appears in any logged line or field — request/header logging MUST redact X-Order-Token (§5.4), mirroring the P0-10 log-exposure assertion pattern (capture the logging surface during the path and assert the secret never appears).
  </action>
  <verify>
    <automated>make host && make verify-protocol && make verify-host</automated>
  </verify>
  <done>P0-11 and P0-12 evidence passes: valid naddr resolves locally with zero relay contact, malformed/wrong-kind/foreign references rejected; ID-only/cross-origin mutations fail, approved bearer/CSRF paths pass, audit capture redacts secrets, request/header logging redacts X-Order-Token on the bearer path, startup readiness gates activation, task/SDK cleanup is cancellation-safe, unsupported topology refuses production-ready mode; literal NIP-15 DTO fixtures and their invalid variants validate per §6.6 — all recorded under P0-11/P0-12 in the evidence manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Decimal/FX boundary model with measured float error + contract-closure gate + final evidence bundle (P0-13, P0-14; QUAL-13, QUAL-14)</name>
  <files>harness/fx.py, harness/registry.py, harness/evidence.py, tests/qualification/test_p0_13_decimal_fx.py, tests/qualification/test_p0_14_contract_closure.py, PINS.md</files>
  <action>
1. harness/fx.py: §3.4 model. FxQuote(currency, currency_per_btc, sat_per_major_unit, providers, observed_at, expires_at) with Decimal values, nonempty providers, sat_per_major_unit = Decimal(100_000_000) / currency_per_btc, observed_at = UTC completion time, expires_at = observed_at + 5 minutes. The host adapter boundary: provider floats cross exactly once via Decimal(str(value)); currency_per_btc is the Decimal arithmetic mean of filtered provider values; the model never calls the host's cached fiat-amount helper and never inherits its int() truncation (STACK.md non-selection; the float-to-Decimal conversion happens only at the adapter boundary). Conversion: amount_minor / 10**currency_decimals * sat_per_major_unit with ROUND_CEILING per line and per shipping component; total is the checked integer sum; total_sat >= 1 required (amountless invoices rejected). Persistence: one order_fx_quotes row per source currency with decimal-string values, provider names, direction/units, observed/expiry timestamps, and source. Rejection-before-reservation: empty, zero, nonfinite, failed, provenance-free (no providers), or stale (expires_at passed) quotes reject checkout before any reservation row or stock increment exists — assert against the plan 01-02 schema (order_fx_quotes/inventory_reservations). Float-boundary measurement: with a seeded, recorded sample of representative provider floats (including values whose binary representation carries error), compare the Decimal(str(v)) mean against exact rational arithmetic and compute the maximum relative error across the sample; the measured bound is recorded into the evidence manifest and REPORT (the spec requires Phase 0 to measure and approve precision/error at this boundary).

2. harness/registry.py: P0-14 closure registries — declarative, literal, and complete: order states + legal transitions (§7.1), shipping/reservation/outbox/inbox state enums (§7.2-7.5), the schema field set the models implement (§4 tables), HTTP routes (§5 tables incl. migration routes), error codes (§5.6), task names (§10 table), settings/env vars (§12), event kinds and their required fixture coverage for this phase's scope (§6: 30402 naddr, 31989/31989-31990 pair, 30017/30018, kind-16 rumors, seal 13, wrap 1059, kind 17, kind 14), release gates (§18 A/B/C with their event-kind scoping), NIP-32 namespace (org.gammamarkets.protocol per §6.8 — pin the exact namespace in PINS.md), and the frozen gammamarkets identifiers (package name, route prefix /gammamarkets, hook names gammamarkets_start/gammamarkets_stop, env var prefix GAMMAMARKETS_, payment correlation prefix gammamarkets:, AAD prefix — per §21.25). The registry documents its modeled-table scope explicitly: a module-level declaration classifying every §4 table as fully modeled by the Phase-1 executable models, present only as a minimal FK-supporting subset, or not modeled in Phase 1 (production-runtime tables outside the executable models' needs, e.g. catalogs, product detail tables, collections, shipping_options, relay_configs/peer_relays, protocol_addresses, merchant_keys, settings/migration_jobs) — making visible to the owner that §4 field closure is proven registry↔implementation (item 4b) with the registry itself as the reviewed middle, unlike routes/states which item 4a diffs directly against the spec text; an §4 table missing from the classification is a registry bug.

3. tests/qualification/test_p0_13_decimal_fx.py (marker db): fractional minor-unit lines (e.g. 1999 minor units of a 2-decimal currency) and mixed-currency shipping components convert with per-component ceilings and never mix currencies arithmetically (§21.9); the checked integer sum rejects overflow; total_sat >= 1 enforced; stale/empty/zero/nonfinite/provenance-free quotes reject before reservation with zero reservation rows created; the five-minute freshness window holds; the persisted quote rows carry decimal strings and provider names; the float-boundary measurement runs and the measured max relative error is recorded into the evidence manifest via a new fx_measurement field surfaced by harness/evidence.py.

4. tests/qualification/test_p0_14_contract_closure.py (marker protocol): the closure gate. (a) Spec parity: extract the route lists from the §5 fenced code blocks and the state-machine lines from §7 of docs/technical-specification.md (read at test time from the repo; anchored regexes over the fenced blocks, with per-literal positive checks as the robust fallback) and diff both directions against harness/registry.py — no undeclared route or state, no missing one; every registered literal (error codes, task names, settings, identifiers, NIP-32 namespace) positively appears in the spec; (b) schema/state parity: every field and state in the registry exists in harness/schema.py and harness/state.py, and neither file declares anything outside the registry (no undeclared field or state); (c) transition completeness: the registry transition table is exhaustive over declared states — every (state, target) pair is classified legal or illegal with no unclassified cell; (d) fixture coverage: golden fixtures exist for every event kind the registry marks as Phase-1-required, and the NIP-89 pair's distinct d values are both present; (e) release scoping: the registry's Release-A set excludes NIP-17/NIP-04-only kinds and the Release-B/C gates map every later-release kind (release-scoped compatibility restrictions, §18); (f) identifier closure: the frozen gammamarkets identifiers appear exactly as declared in the harness (route prefix, external-id correlation prefix used by the saga/host probes, env var prefixes) with no undeclared variant spelling; (g) evidence closure (complete-profile runs only): assert over the evidence plugin's in-session results accumulator, consumed via the evidence_results fixture the plugin exposes together with the single shared p0_coverage derivation used by the serializer (see item 5), that the p0_coverage map contains P0-01 through P0-14, each with at least one passed test and none missing — P0-01..P0-14 have recorded evidence (roadmap success criterion 5); do not read evidence/manifest.json from disk inside the run — the plugin serializes it at session end, so an in-session file read would see absent or stale data, and the accumulator is the run's live truth; the assertion self-skips (pytest.skip, reported as skipped — never as passed) whenever a test-selection filter is active: the evidence fixture exposes the session's marker expression and -k filter (read from pytest config), and any non-empty selection (e.g. make verify-protocol's -m protocol slice) means the session's accumulator cannot be complete, so (g) does not apply — assertions (a)–(f)+(h) remain the protocol-scoped closure assertions and must pass in every subset that collects this module; under the unfiltered complete profile (make verify) the assertion executes and finds the accumulator complete because alphabetical collection places test_p0_14_contract_closure after every other qualification module; (h) topology: the unsupported-topology refusal rule from plan 01-02's probe is registered (CockroachDB and multi-process SQLite are not claimed, §14).

5. harness/evidence.py (extend) + PINS.md (complete): expose the plugin's in-session per-test results accumulator as a pytest fixture (evidence_results) together with the single shared p0_coverage derivation used by both the serializer and the P0-14 closure assertion (item 4g) — the closure consumes the live accumulator, never the manifest file, because the plugin serializes evidence/manifest.json at session end and an in-session file read would see absent or stale data — and expose the session's active selection state (marker expression and -k filter, read from pytest config) through the same fixture so the closure assertion can scope itself to complete-profile runs (item 4g's self-skip). Extend the report renderer to include the measured FX float-boundary error, the P0-01..P0-14 coverage table with pointers, the clean-pass policy statement (single-run results only; reruns diagnose but never flip a recorded outcome, D-10), links to the CI artifacts for each blocking profile's uploaded manifest/report plus the macOS advisory run (D-09: raw output stays a CI artifact, linked from the report), and the pending Approval section content for the owner sign-off required before Phase 2 planning (D-11) — pin the NIP-32 namespace (org.gammamarkets.protocol unless the spec revision says otherwise) and record the FX measured bound. Add PINS.md's qualification-results section (spec §2: PINS.md MUST record executable security, FFI, and per-relay ACK results — results live in PINS.md, not pointers alone): the normalized P0-02 security/FFI/crypto status and the P0-04 per-relay ACK status (positive OK / negative OK / timeout per tested profile), each as a concise pass/fail summary with pointers to the evidence/manifest.json runs carrying the full per-test detail. The final committed evidence bundle remains PINS.md + evidence/manifest.json + evidence/REPORT.md + golden fixtures (D-09); raw output stays a CI artifact. Final-bundle commit rule: a local make verify overwrites evidence/manifest.json + evidence/REPORT.md with the executing machine's single-profile result — acceptable during development (D-12) — but the durable committed bundle is refreshed at phase end from the final complete CI matrix: commit the canonical blocking-profile job's uploaded manifest artifact (designate the Linux x86_64 + PostgreSQL profile) as evidence/manifest.json and render the committed REPORT.md linking all four blocking profiles' plus the macOS advisory job's CI artifacts, so the durable bundle plus its linked artifacts covers the complete matrix; never commit a developer-machine single-profile run as the durable evidence.
  </action>
  <verify>
    <automated>make host && make verify-db && make verify-protocol && make verify</automated>
    <human-check>Owner approval gate (D-11): review the final PINS.md (pins, platform matrix, SDK artifact identities and provenance, the qualification-results section — normalized P0-02 security/FFI/crypto status and P0-04 per-relay ACK results per spec §2 — NIP-32 namespace, measured FX float-boundary bound) and the evidence/REPORT.md P0-01..P0-14 coverage summary, then record explicit approval in PINS.md's Approval section. Clean CI on all four blocking profiles alone does not unlock Phase 2 planning.</human-check>
  </verify>
  <done>P0-13 and P0-14 evidence passes: Decimal conversion with per-component ceilings, provenance, five-minute freshness, rejection-before-reservation, and a measured recorded float-boundary error; the closure gate proves registry/spec/schema/state/fixture parity both directions, exhaustive transition classification, release-scoped restrictions, identifier closure, and a complete P0-01..P0-14 evidence map; PINS.md and the report carry everything the owner needs for the D-11 approval decision, which remains explicitly pending until the owner signs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden fixtures -> validation chain | fixtures are trusted test input; a weakened fixture weakens the qualification |
| local relays <-> SDK client | in-process loopback traffic; party-scoped routing must never leak across sets |
| naddr input -> local resolver | untrusted bech32 input incl. embedded relay hints must not cause network egress |
| probe routes on host app | probe router exists only inside the test fixture; bearer/CSRF boundaries are under test |
| optional external relay (wss://nostr.net) | opt-in outbound connection carrying only ephemeral synthetic data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | optional wss://nostr.net smoke publish | medium | mitigate | Env-gated, skipped by default; ephemeral throwaway key; non-sensitive synthetic event; advisory only and never a substitute for deterministic local relay evidence (§21.27, D-16 defense-in-depth analogy). |
| T-03-01 | Information disclosure | NIP-17 plaintext or conversation keys in logs | high | mitigate | P0-05 log-capture assertions fail on any plaintext or key leak across wrap/unwrap/reject paths. |
| T-03-02 | Spoofing/SSRF | naddr relay hints triggering server-side fetches | high | mitigate | P0-11 asserts zero connections to hint relays (silent-relay probe) and the resolver is local-only by construction — no network code path exists to reach a hint. |
| T-03-03 | Elevation | probe routes exposing an admin surface on the host app | medium | mitigate | Probe router is mounted only inside the test fixture app, never in committed runtime code; ID-only/cross-origin probes prove the rejection side of the boundary. |
| T-03-04 | Tampering | golden fixtures hand-edited to weaken validation | medium | mitigate | Fixture directories carry a regeneration README with recorded seed; tamper and seeded-generated matrices re-derive rejections; P0-14 counts fixture coverage per registry. |
| T-03-05 | Repudiation | closure gate asserting against a stale specification copy | low | mitigate | The closure test reads docs/technical-specification.md from the repo at test time; the spec is the normative in-repo artifact. |
| T-03-06 | Tampering | FX model inheriting host float truncation | high | mitigate | The model never calls the host's cached fiat helper (asserted structurally in review and by the measured-bound test); conversion is Decimal-only past the single str() boundary crossing. |
</threat_model>

<verification>
- `make verify-protocol` — P0-04/P0-05/P0-11 and the P0-14 protocol-scoped closure assertions.
- `make verify-db` — P0-13 FX model assertions (rejection-before-reservation runs against the schema).
- `make verify` — the complete profile: every p0 module, evidence manifest, and report; the P0-14 evidence-closure assertion (complete-profile only — it self-skips under any marker/k selection, so subset runs like make verify-protocol never fail on it) passes only when the run's live accumulator shows P0-01..P0-14 all recorded and passed.
- CI: all four blocking profiles pass clean over the complete suite (D-03, D-10); macOS advisory run recorded as visible-but-non-blocking (D-04).
- `make verify-sdk` still passes (P0-02 regression surface intact after sdk.py extensions).
</verification>

<success_criteria>
- Protocol fixtures prove NIP-89 routing, NIP-17 wrap validation/routing, literal NIP-15 DTOs, and release-scoped compatibility restrictions (roadmap success criterion 4).
- The FX boundary is proven with a measured and recorded float-boundary error (QUAL-13/P0-13).
- The contract-closure gate passes with no undeclared state, field, route, identifier, or release dependency, and P0-01..P0-14 all have recorded evidence (QUAL-14/P0-14, roadmap success criterion 5).
- The evidence bundle is complete and committed (PINS.md + manifest + report + golden fixtures, D-09) and the owner approval section is explicitly pending for the D-11 gate.
- No production runtime features were added anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/01-conformance-profile-contract-phase-0/01-03-SUMMARY.md` when done
</output>
