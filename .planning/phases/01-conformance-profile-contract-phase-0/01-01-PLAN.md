---
phase: 01-conformance-profile-contract-phase-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - .gitignore
  - Makefile
  - PINS.md
  - tools/checkout_host.py
  - harness/__init__.py
  - harness/evidence.py
  - harness/pins.py
  - harness/db.py
  - harness/schema.py
  - harness/tx.py
  - harness/sdk.py
  - harness/host.py
  - harness/relay.py
  - tests/conftest.py
  - tests/qualification/test_p0_01_pins.py
  - tests/qualification/test_p0_02_sdk_security.py
  - tests/qualification/test_p0_03_host_contract.py
  - tests/qualification/test_p0_06_transactions.py
  - .github/workflows/qualification.yml
  - evidence/manifest.json
  - evidence/REPORT.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One canonical command (make verify) runs the complete qualification suite for the selected profile and fails on any non-passing test (D-07, D-10)."
    - "PINS.md records LNbits e336fe1, nostr-sdk 0.44.8 wheel filename and SHA-256 per claimed platform, release-source and native provenance, lockfile paths, and the Python 3.12-only claim (D-01, D-02, QUAL-01/P0-01)."
    - "The installed nostr-sdk artifact on the executing platform matches the recorded host-lock hashes and the pinned host checkout is exactly e336fe1; any mismatch fails the suite (P0-01, no silent downgrade per D-01)."
    - "The tested SDK binary rejects repeated invalid/known-ID events and oversized NIP-44 inputs before trusted processing, and paused-signing AUTH bursts remain bounded (QUAL-02/P0-02, D-16)."
    - "Host probes prove invoice extension/external-id metadata, wallet correlation, exact external-id lookup, owned-handle listener cancellation, and that callback delivery is not durable across restart (QUAL-03/P0-03)."
    - "A complete run emits evidence/manifest.json and evidence/REPORT.md with a P0-01..P0-14 coverage map and no credentials (D-09)."
  artifacts:
    - path: "pyproject.toml"
      provides: "uv project; requires-python >=3.12,<3.13 (D-02); lnbits path source; nostr-sdk==0.44.8 exact pin"
      contains: "nostr-sdk==0.44.8"
    - path: "Makefile"
      provides: "canonical verification command with named subsets verify, verify-fast, verify-sdk, verify-db, verify-protocol, verify-host (D-06, D-07, D-12)"
    - path: "PINS.md"
      provides: "pin freeze, platform matrix, SDK artifact hashes, provenance, evidence pointers, approval section (D-01, D-02, D-03, D-04, D-09, D-13, D-15)"
    - path: "tools/checkout_host.py"
      provides: "idempotent LNbits checkout at e336fe14b841d6f0c940e75b3d343e3ab5cf8433 with commit verification"
    - path: "tests/conftest.py"
      provides: "markers fast/sdk/db/protocol/host, evidence plugin registration, LNBITS_DATABASE_URL dialect switch, host-checkout env wiring"
    - path: "harness/evidence.py"
      provides: "pytest plugin recording normalized results to evidence/manifest.json and rendering evidence/REPORT.md"
    - path: "tests/qualification/test_p0_01_pins.py"
      provides: "P0-01 pin/provenance verification"
    - path: "tests/qualification/test_p0_02_sdk_security.py"
      provides: "P0-02 SDK security/FFI/crypto probes"
    - path: "tests/qualification/test_p0_03_host_contract.py"
      provides: "P0-03 host invoice/listener/task probes"
    - path: ".github/workflows/qualification.yml"
      provides: "4 blocking Linux profiles + macOS advisory smoke (D-01, D-03, D-04)"
  key_links:
    - from: "Makefile"
      to: "tests/conftest.py + harness/evidence.py"
      via: "make verify -> uv run pytest -> evidence plugin writes manifest and report"
      pattern: "verify:"
    - from: "tests/qualification/test_p0_01_pins.py"
      to: ".cache/lnbits/uv.lock (host checkout)"
      via: "wheel SHA-256 parity check against the pinned host lock"
      pattern: "nostr-sdk"
    - from: ".github/workflows/qualification.yml"
      to: "Makefile"
      via: "each CI profile job runs make verify with its LNBITS_DATABASE_URL and uploads evidence artifacts"
      pattern: "make verify"
---

## Phase Goal

**As a** merchant-operator, **I want to** prove the exact LNbits/SDK/platform/database profile with reproducible qualification evidence before any production runtime is built, **so that** merchants' real funds are never exposed to an unqualified contract.

(Story source: REQUIREMENTS.md user story "As an operator, I can prove the exact LNbits/SDK/deployment profile before merchants use real funds", adapted to the Phase 1 qualification-harness boundary.)

<objective>
Build the permanent qualification harness as a walking skeleton (thinnest end-to-end qualification slice: pin recording -> one real SDK probe -> one real database transaction model check -> one evidence record emitted), then expand it to freeze pins and artifact provenance (QUAL-01/P0-01) and qualify the SDK security boundary (QUAL-02/P0-02) and the host contract boundary (QUAL-03/P0-03).

This plan implements workstream 01-01 from the ROADMAP: "Freeze pins, artifact provenance, and host/SDK contract harness".

NO production extension runtime code: no merchant/catalog/checkout/worker/UI features, no FastAPI routes beyond probe fixtures, no migrations on production schemas. The harness is permanent regression infrastructure, not disposable qualification code (D-05).

Purpose: Everything later phases build depends on the pinned host/SDK/database contract being proven reproducible. A silent downgrade, an unverified SDK binary, or an assumed host behavior would poison every later vertical slice.

Output: uv-managed qualification harness (pyproject.toml, Makefile with canonical command, harness/ support package, tests/qualification/ p0-named test modules), tools/checkout_host.py, PINS.md, evidence bundle tooling (evidence/manifest.json + evidence/REPORT.md), CI workflow with the full blocking matrix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-conformance-profile-contract-phase-0/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@docs/technical-specification.md

Normative spec sections this plan implements: §2 (pinned inputs + PINS.md requirements), §2.1 (host integration boundary), §8.2 step 1 (conditional reservation SQL), §8.3 (settlement trigger/listener contract), §9.1 (NostrTransport interface, deterministic local relays authoritative), §10 (background task lifecycle), §14 (transaction adapter dialect rules), §17 (host/SDK contract tests), §21 decisions 23/26/27 (SDK qualification, transport adapters, external relay smoke), §22 P0-01/P0-02/P0-03.

The pinned LNbits source (commit e336fe1, tag v1.6.2-rc1) is the reference for host API surfaces. A verified local copy exists at /Users/bk/github/gamma-markets-research/lnbits (read-only reference; the harness clones its own copy via tools/checkout_host.py). Known-good reference points verified during planning: lnbits/task_manager.py exposes TaskManager.cancel_task, TaskManager.create_permanent_task, and TaskManager.register_invoice_listener(coro, name=...); lnbits/core/services/payments.py exposes create_invoice(..., extension=..., external_id=...); lnbits/db.py exposes Database and Connection with the auto-committing helpers the transaction adapter must avoid; tests/conftest.py in the host shows the app-boot pattern (create_app, asgi_lifespan.LifespanManager, first_install, FakeWallet, LNBITS_DATA_FOLDER, LNBITS_DATABASE_URL). Verify every signature against the installed pinned checkout before coding against it; where the nostr-sdk 0.44.8 Python API surface is genuinely unknown, inspect the installed package (python -c "import nostr_sdk; print(dir(nostr_sdk))", importlib.metadata, help()) before writing code — never invent signatures.

nostr-sdk 0.44.8 wheel identities recorded in the pinned host uv.lock (source of truth for P0-01): manylinux_2_17_x86_64 sha256 e6b2f1b44ae95b35712008c2e0335ab833a30545a80c4afa45992b439b20e66b; manylinux_2_17_aarch64 sha256 7b7a684afbfc9118aa2dd3adc7f06fa0d0f872e7ccfdf7542d2ba649f4b3689b; macosx_11_0_arm64 sha256 d781526da09550a8ca7b11abf0ee270bda53fabe191a2abfc8851dd1f3f4235c; musllinux variants also present in the lock. Parse the lock at runtime rather than hardcoding these values.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end qualification slice — pins recorded, one real SDK probe, one real DB transaction check, one evidence record (walking skeleton)</name>
  <reversibility rating="costly">Implements D-01's platform claim recording: broadening or narrowing the claimed platform matrix later changes CI coverage, published support policy, and recorded artifact provenance (rating carried from CONTEXT.md D-01).</reversibility>
  <precondition>git, network access, and uv are available on the executing machine; uv can install a Python 3.12 toolchain.</precondition>
  <files>pyproject.toml, uv.lock, .gitignore, Makefile, PINS.md, tools/checkout_host.py, harness/__init__.py, harness/evidence.py, harness/pins.py, harness/db.py, harness/schema.py, harness/tx.py, harness/sdk.py, tests/conftest.py, tests/qualification/test_p0_01_pins.py, tests/qualification/test_p0_02_sdk_security.py, tests/qualification/test_p0_06_transactions.py, evidence/manifest.json, evidence/REPORT.md</files>
  <action>
Wire ONE end-to-end qualification path through every layer this phase touches, production-quality and permanent (D-05) — this slice becomes the skeleton of the final harness; functionality gaps are acceptable, architectural gaps are not.

Tracer-surface justification (monitored context risk): this task deliberately carries a wide file surface (19 paths including harness/pins.py) because each path is one layer the walking skeleton must prove wired — build/lock (pyproject.toml, uv.lock, .gitignore), command surface (Makefile), host provenance (tools/checkout_host.py), pins (PINS.md + harness/pins.py), evidence (harness/evidence.py, evidence/manifest.json, evidence/REPORT.md), database (harness/db.py, schema.py, tx.py), SDK (harness/sdk.py), test surface (tests/conftest.py + the three tracer test modules). Trimming creation into a later task would leave the tracer asserting a layer it never wired — e.g. deferring harness/pins.py's recording helper to Task 2 would break the tracer's tested-binary-identity -> evidence link and force Task 2 to rework green tracer code — so the surface stays and the slice stays coherent. The cost is Task 1 context pressure, which the executor treats as a monitored risk: keep each file's tracer contribution to the minimum the end-to-end path needs (helpers only, no speculative generality), and if pressure mounts, shrink per-file scope rather than dropping a layer or deferring file creation.

1. tools/checkout_host.py: idempotent script that clones https://github.com/lnbits/lnbits into the directory named by env var GAMMA_QUAL_LNBITS_DIR (default .cache/lnbits), checks out commit e336fe14b841d6f0c940e75b3d343e3ab5cf8433 (detached), and hard-fails unless git rev-parse HEAD equals that hash — no silent downgrade and no partial use of a wrong revision (D-01, P0-01). Print the verified commit. Never modify the read-only research checkout at /Users/bk/github/gamma-markets-research/lnbits; the harness always uses its own clone.

2. pyproject.toml: [project] name "gammamarkets", requires-python ">=3.12,<3.13" — the narrower Python 3.12-only claim per D-02, even though the host permits 3.10–3.12; PINS.md must state this explicitly. Dependencies: lnbits as a path dependency resolved via [tool.uv.sources] to the checkout directory (the pinned host is a hatchling-built package: build-system hatchling, wheel packages "lnbits"), nostr-sdk pinned exactly ==0.44.8 (stricter than the host's ~=0.44.0 so the lock cannot drift to a newer 0.44.x — D-13), and dev group: pytest, pytest-asyncio, ruff. Generate uv.lock with uv lock after the host checkout exists. Add .gitignore entries for .cache/, .venv/, __pycache__/, *.pyc — note that evidence/ IS committed (D-09).

3. Makefile (D-06, D-07, D-12): target "host" runs tools/checkout_host.py. Canonical targets, all depending on host and runnable from repo root: "verify" runs the complete suite for the selected profile and emits evidence; "verify-fast" (pytest -m fast), "verify-sdk" (-m sdk), "verify-db" (-m db), "verify-protocol" (-m protocol), "verify-host" (-m host) are the named subsets. The suite is one pytest surface: the database dialect is selected by LNBITS_DATABASE_URL exactly like the pinned host (unset/empty = SQLite profile), mirroring the host's Makefile test targets and uv usage.

4. tests/conftest.py: register markers fast, sdk, db, protocol, host (all of them now — later plans must not need to touch this file); set asyncio_mode=auto and pythonpath ["."]; wire env conventions (LNBITS_DATABASE_URL read once at session start; GAMMA_QUAL_LNBITS_DIR default .cache/lnbits; LNBITS_BACKEND_WALLET_CLASS=FakeWallet and a tmp LNBITS_DATA_FOLDER only inside the host fixtures, mirroring the host's tests/conftest.py); register the evidence plugin from harness/evidence.py.

5. harness/evidence.py: pytest plugin + report renderer. Record per test: node id, outcome, duration, markers, the P0 id derived from the module filename pattern test_p0_NN_*, and an optional subcheck-attribution detail populated by the P0-02 probes (Task 3) recording which checks are SDK-internal probes versus admission-modeled defense-in-depth checks — REPORT.md renders this split as the D-16 disclosure so the owner can see what extension-level checks do and do not waive. Build the manifest at evidence/manifest.json with schema version, UTC timestamp, the exact verify command, a profile block (python version, platform.machine + sys.platform, database dialect name only — never the raw LNBITS_DATABASE_URL which may contain credentials), a pins block (nostr-sdk version, lnbits commit), per-test results, a summary with a p0_coverage map of P0-01..P0-14, and a statement that reruns do not flip recorded outcomes (D-10 clean-pass policy — the tooling performs single runs only, no retry loop). Render evidence/REPORT.md from the manifest: profile, pins summary, P0 coverage table, failures. Raw pytest output is not committed (D-09).

6. harness/db.py + harness/schema.py + harness/tx.py: the dialect-switching database slice. db.py provides a session-scoped fixture factory that yields a fresh schema database per test (SQLite: tmp file via the host Database class; PostgreSQL: selected by LNBITS_DATABASE_URL) and creates the DDL from schema.py. schema.py defines ONLY the minimal normative subset this slice needs, with dialect-appropriate DDL: products (id, stock_on_hand NULL-able, stock_reserved, deleted_at), orders (id, merchant_id, state), inventory_reservations (id, product_id FK, order_id FK, quantity > 0, state, expires_at) per §4.12. tx.py implements the §14 SQLite path of the transaction adapter: one Database.connect() context, BEGIN IMMEDIATE, raw parameterized statements only, explicit commit/rollback — never the host's auto-committing Connection/Database helper methods inside the transaction (their use is forbidden and tested in plan 01-02). Enable SQLite foreign key enforcement per connection so FK behavior is testable (P0-06 requirement).

7. harness/sdk.py (minimal): key generation, event construction/sign/verify, and a NIP-44 encrypt/decrypt round-trip helper — all through nostr-sdk only, no custom crypto (STACK.md non-selection). First verify the installed 0.44.8 API surface (imports, Keys/EventBuilder/sign/verify, NIP-44 module names) against the installed package and record what you found in the SUMMARY; never code against an assumed signature.

8. Tracer test modules (each carries its P0 marker): tests/qualification/test_p0_01_pins.py (marker fast) — asserts importlib.metadata version of nostr-sdk is exactly 0.44.8, requires-python in pyproject.toml is ">=3.12,<3.13", the running interpreter is 3.12 (D-02), the host checkout HEAD is e336fe1 via the checkout helper, and PINS.md contains the pinned identities. tests/qualification/test_p0_02_sdk_security.py (marker sdk) — the one real SDK probe: FFI import, generate two keypairs, build and sign an event, verify it, tamper one field and assert verification fails, NIP-44 encrypt/decrypt round-trip, and record the tested binary identity (installed native library filename + SHA-256) into the evidence pins block via a helper in harness/pins.py (create the module with just this recording helper; plan Task 2 extends it). tests/qualification/test_p0_06_transactions.py (marker db) — the one real DB transaction model check per §8.2 step 1: inside one BEGIN IMMEDIATE transaction run the conditional reservation UPDATE on products with the stock_on_hand - stock_reserved >= qty guard, a compare-and-swap UPDATE on orders WHERE state='received', and insert a held reservation; then assert (a) forcing the CAS to lose (state already advanced) rolls back the entire transaction — stock_reserved unchanged and no reservation row; (b) an orphan inventory_reservations insert violates the FK; (c) the winning path leaves exactly one held reservation and stock_reserved incremented.

9. PINS.md (initial): sections for normative pins (GammaMarkets market-spec 5dc79c5, Nostr NIPs a2494f4f81d46684e5814a9bf35e2b1df978f955, NIP-44 v2 payload only, LNbits v1.6.2-rc1 at e336fe1, nostr-sdk 0.44.8 candidate), the platform claim (blocking: Linux x86_64 + Linux ARM64, each with SQLite and PostgreSQL = 4 profiles per D-01/D-03; advisory: macOS ARM64 smoke per D-04; Python 3.12 only per D-02), SDK artifact identities (wheel filename + SHA-256 per platform tag, parsed from the host checkout's uv.lock at verification time — do not hand-copy hashes into prose beyond documenting the source), evidence pointers (evidence/manifest.json, evidence/REPORT.md, CI artifacts), and an Approval section marked pending for the owner sign-off required before Phase 2 planning (D-11).

Commit nothing outside the harness, PINS.md, Makefile, and evidence outputs. No production extension runtime code, no UI, no deployment scaffolding.
  </action>
  <verify>
    <automated>make verify</automated>
    <human-check>Review PINS.md initial pin set and the committed dependency list (lnbits path source at e336fe1, nostr-sdk==0.44.8, pytest stack): this is the package-legitimacy confirmation for the initial install set. Automated hash parity against the host lock lands in Task 2.</human-check>
  </verify>
  <done>make verify passes end-to-end on the SQLite profile: host checkout verified at e336fe1, uv sync resolves the pinned set, all tracer tests pass, evidence/manifest.json exists with the three tracer test results and a P0 coverage map, evidence/REPORT.md renders, PINS.md records pins and the platform claim, and the whole path ran from one command (D-07).</done>
</task>

<task type="auto">
  <name>Task 2: Freeze pins and artifact provenance verification + canonical subsets + CI blocking matrix (P0-01, QUAL-01)</name>
  <reversibility rating="costly">Freezing nostr-sdk 0.44.8 as the recorded pin: any later pin change reopens the complete qualification profile and requires explicit owner approval (D-13, D-14); activating the D-15 source-built path additionally introduces a maintained native toolchain obligation.</reversibility>
  <files>harness/pins.py, tools/checkout_host.py, tests/qualification/test_p0_01_pins.py, PINS.md, .github/workflows/qualification.yml, Makefile</files>
  <action>
Expand the tracer's pin recording into the full P0-01 provenance verification suite and the CI topology that owns the complete blocking matrix (D-01, D-03, D-04, D-06, D-07, D-09, D-12, D-15).

1. harness/pins.py (extend): TOML parsing (stdlib tomllib) of the host checkout's uv.lock to extract every nostr-sdk 0.44.8 wheel entry (filename, URL, sha256, platform tag) plus its sdist entry if present; mapping of the executing machine to its expected platform tag (manylinux/musllinux x86_64/aarch64, macosx arm64 — derive from sysconfig/platform tags, and record which wheel the current environment installed); a helper returning the installed native library file (locate the compiled .so/.dylib inside the installed nostr-sdk package via importlib.metadata distribution files) and its SHA-256; helpers recording our own uv.lock path + file SHA-256 and the harness's full lock resolution — every direct and transitive package with name/version/hash, not just nostr-sdk — into the evidence manifest pins block, so divergence between the harness's transitives and the host checkout's resolution is visible at review instead of silently accepted. pins.py reads only the harness's own verified checkout (.cache/lnbits at e336fe1); when verifying host API signatures during implementation, use that pinned checkout — never the local read-only research checkout at /Users/bk/github/gamma-markets-research/lnbits, which sits at a different revision (74cccac) and is not the qualified source.

2. tests/qualification/test_p0_01_pins.py (extend, marker fast): (a) the installed nostr-sdk version is exactly 0.44.8; (b) the harness uv.lock contains a nostr-sdk==0.44.8 entry whose sha256 set for the current platform tag equals the host uv.lock entry for the same tag — parity failure blocks (no silent downgrade, D-01); (c) the host checkout HEAD is e336fe1; (d) requires-python is >=3.12,<3.13 and the interpreter is 3.12.x (D-02); (e) the installed native library hash is recorded into the evidence pins block (binary provenance, "record the tested binary"); (f) the nostr-sdk sdist URL+sha256 from the host lock is recorded in PINS.md, and when the sdist is fetched (cached under .cache/) its sha256 is verified and its Cargo.lock is parsed to record the native Cargo dependency revisions (at minimum the secp256k1 and chacha20poly1305-family crate pins) — release-source revision and native provenance per spec §2; (g) PINS.md contains the 4-profile blocking matrix, the macOS advisory profile, the Python 3.12-only statement, and the D-15 contingency criteria (source-built SDK allowed only with pinned native revision + reproducible build + hashed artifacts + recorded review evidence + all blocking tests passing, and only with owner approval); (h) evidence manifest pins block matches the recorded values.

3. .github/workflows/qualification.yml: modeled on the pinned host's .github/workflows/ci.yml + tests.yml conventions (ubuntu-24.04 runners, postgres:18 service with health checks, LNBITS_DATABASE_URL + LNBITS_BACKEND_WALLET_CLASS=FakeWallet env, uv-based execution). Jobs: a lint job (uv run ruff check .); four blocking profile jobs — ubuntu-24.04 (x86_64) x {SQLite, PostgreSQL} and ubuntu-24.04-arm (ARM64) x {SQLite, PostgreSQL} (D-01, D-03) — each running make host then make verify with its LNBITS_DATABASE_URL and uploading evidence/manifest.json + evidence/REPORT.md plus raw pytest output as named CI artifacts (D-09: raw logs stay CI artifacts, only the normalized bundle is committed); one macOS ARM64 advisory job (macos-latest, SQLite, continue-on-error: true so failure is visible but non-blocking, D-04). Trigger on push and pull_request. Python 3.12 everywhere.

4. Makefile: if needed, adjust subset wiring so every named subset is a pure pytest -m selection over the same single test tree (one canonical command surface, D-07). Subset reruns are the impact-based development loop (D-12); only make verify and the CI profiles produce durable evidence.

5. PINS.md (extend): record the supported installation/lockfile path sequence (checkout_host.py -> uv sync) and both lock identities (harness uv.lock hash; host checkout path + commit); surface the harness's full uv.lock resolution recorded by pins.py into the manifest pins block so host-transitive divergence is visible in the durable bundle. State that pins are surfaced per spec §2 and that any candidate other than host-resolved 0.44.8 requires an explicit spec decision and identical qualification (D-13, D-14).
  </action>
  <verify>
    <automated>make host && uv run pytest tests/qualification/test_p0_01_pins.py -q && uv run --with pyyaml python -c "import yaml; yaml.safe_load(open('.github/workflows/qualification.yml')); print('workflow-yaml-ok')"</automated>
  </verify>
  <done>P0-01 suite passes on the executing platform: installed SDK identity, wheel hash parity against the host lock, native library hash, sdist/Cargo provenance, Python claim, and host revision all verified and recorded in PINS.md and the evidence manifest; CI workflow defines exactly the 4 blocking profiles plus the non-blocking macOS advisory job; named subsets all resolve as pytest -m selections from repo root.</done>
</task>

<task type="auto">
  <name>Task 3: SDK security probes + host contract probes with deterministic local relay fixture (P0-02, P0-03; QUAL-02, QUAL-03)</name>
  <files>harness/relay.py, harness/host.py, harness/sdk.py, tests/qualification/test_p0_02_sdk_security.py, tests/qualification/test_p0_03_host_contract.py, pyproject.toml</files>
  <action>
Build the executable probes that qualify the SDK security boundary and the host contract boundary. D-13/D-14 apply throughout: if any blocking security, FFI, crypto, or ACK check fails against nostr-sdk 0.44.8, the phase stays blocked — extension-level input checks are defense in depth only and cannot waive an SDK-internal regression (D-16). First verify every nostr-sdk and host API signature against the installed package/checkout (per the context section's verify-first rule) before coding.

1. harness/relay.py: deterministic local Nostr relay fixture over the websockets library (add websockets to the dev dependency group in pyproject.toml). A small asyncio server bound to 127.0.0.1 on an ephemeral port, speaking the NIP-01 subset: configurable modes ACCEPTING (reply OK true), REJECTING (reply OK false with a message), SILENT (accept connection, never reply — produces client timeout), and AUTH_FLOOD (send repeated AUTH challenges, up to a configurable count). It records every received EVENT message and every connection/disconnection for later assertions (the unlisted-target proof), and exposes a start/stop async context manager. This fixture is the authoritative relay test surface per §9.1; the optional wss://nostr.net smoke is out of scope for this task and non-authoritative by policy (§21.27).

2. tests/qualification/test_p0_02_sdk_security.py (extend, marker sdk) — SDK security/FFI/crypto probes (P0-02): (a) invalid-event rejection: events with tampered content (id mismatch), tampered signatures, and malformed ids are rejected by the SDK verification path before any trusted processing — assert rejection is a clean error, not a crash; (b) known-ID repetition: feed the same valid event id through the admission helper repeatedly and assert exactly-once processing via an id-registry dedupe modeled in harness/sdk.py — documented as extension-side defense in depth per D-16, while SDK-internal regressions remain blocking; (c) oversized NIP-44 input: decrypt with a multi-megabyte crafted ciphertext is rejected before full decode/allocation — assert bounded wall time and no unbounded memory growth (use tracemalloc for the allocation assertion); (d) paused-signing AUTH boundedness: connect an SDK client to the AUTH_FLOOD relay with signing not performed (no auth credential supplied — verify how the installed SDK exposes relay AUTH and whether it can be left unanswered) and assert that a large bounded number of challenges produces bounded work, no exception storm, and a client that remains responsive (P0-02: AUTH challenge work remains bounded when signing is paused); (e) record the tested binary identity in the evidence pins block; (f) subcheck attribution: each P0-02 subcheck records into the evidence manifest, via the attribution detail from Task 1's plugin, whether it is an SDK-internal probe (blocking under D-13/D-16) or an admission-modeled defense-in-depth check — the known-ID dedupe of (b) is admission-modeled, the remaining subchecks are SDK-internal — so the report's D-16 disclosure is mechanical rather than prose. If the installed SDK cannot express a required probe (for example no per-relay result surface or no way to observe AUTH behavior), STOP and record it as a blocking qualification failure under D-13 — do not silently narrow the claim.

3. harness/host.py: host app fixture following the pinned checkout's tests/conftest.py pattern (read it first): isolated tmp LNBITS_DATA_FOLDER, LNBITS_BACKEND_WALLET_CLASS=FakeWallet, LNBITS_DATABASE_URL per selected profile, create_app + asgi_lifespan.LifespanManager + first_install bootstrap, wallet/account creation helpers (create_user_account/create_wallet from lnbits.core.crud/services as used by the host's own tests — verify signatures at e336fe1). The fixture owns no production routes — it exists so probes can exercise the real host.

4. tests/qualification/test_p0_03_host_contract.py (marker host) — host contract probes against the real pinned host with FakeWallet funding (P0-03): (a) invoice metadata: call the host invoice service with extension="gammamarkets", external_id="gammamarkets:" plus a fresh UUID, a sat amount, and extra tag {"tag":"gammamarkets","order_id":<uuid>}; assert the persisted core payment row records the extension, the exact external_id, the wallet id, and the amount, and that exact external_id lookup retrieves it (the §8.2 reconciliation key); (b) invoice-listener lifecycle: register an invoice listener via task_manager.register_invoice_listener(coro, name="gammamarkets") — verify the exact registration API at e336fe1 — settle the FakeWallet invoice through the host's test settlement path (see how the host's own tests move a payment to paid) and assert the registered callback receives the Payment carrying our extension/external_id; also register an unrelated named task and assert it is untouched by our cancellation; (c) owned-handle cancellation: cancel only the extension-owned handle via task_manager.cancel_task and assert the listener stops while unrelated tasks survive — the host's cancel-everything API is never invoked by extension code (§10; the probe asserts scoped cancellation only); (d) no durable callback delivery: boot a second app instance simulating restart, settle another invoice with the same external_id shape, assert the previously registered listener receives nothing (registration is in-memory only) and that the settled payment remains discoverable by exact external_id query — proving the reconciliation path (query, not replayed callback) is the only recovery route, as §8.3/§8.7 require. Document observed lifecycle and failure semantics in the module docstring and SUMMARY for the report.
  </action>
  <verify>
    <automated>make host && make verify-sdk && make verify-host</automated>
  </verify>
  <done>P0-02 probes pass: invalid/known-ID events rejected before trusted processing, oversized NIP-44 rejected before full decode/allocation, AUTH bursts bounded with signing paused, tested binary recorded. P0-03 probes pass: invoice extension/external-id/wallet metadata persisted and exactly queryable, listener registration + owned-handle cancellation proven, unrelated tasks untouched, and callback non-durability across restart demonstrated with the query path as the only recovery route. All results recorded in the evidence manifest under P0-02/P0-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry -> venv | uv-resolved dependencies (nostr-sdk wheel, lnbits path source, dev tools) cross into the build environment |
| github.com -> .cache/lnbits | LNbits source clone enters the qualification environment |
| local relay fixture <-> SDK client | in-process test traffic on 127.0.0.1 |
| test process -> database | LNBITS_DATABASE_URL may carry credentials for the PostgreSQL profile |
| harness -> evidence bundle | test results and pin identities are written into committed artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | uv dependency installs (nostr-sdk wheel, lnbits path source, dev tooling) | high | mitigate | Exact nostr-sdk==0.44.8 pin; P0-01 verifies installed wheel sha256 parity against the pinned host uv.lock; host checkout verified by content-addressed commit id; no package added without a recorded pin; owner reviews the final PINS.md before Phase 2 (D-11). No phase RESEARCH.md package-legitimacy table exists (SKIP-RESEARCH mode) — initial set confirmed by the Task 1 human-check, automated provenance by Task 2. |
| T-01-01 | Tampering | tools/checkout_host.py host revision | high | mitigate | Hard-fail unless git rev-parse HEAD equals e336fe14b841d6f0c940e75b3d343e3ab5cf8433; no silent downgrade (D-01). |
| T-01-02 | Spoofing | CI clone of LNbits remote | medium | mitigate | Git commit ids are content-addressed; post-clone hash verification makes a spoofed or truncated clone fail the suite. |
| T-01-03 | Information disclosure | committed fixtures, PINS.md, evidence bundle | medium | mitigate | All keys in fixtures are freshly generated synthetic test keys, never production nsec; evidence records hashes/versions only; manifest never records LNBITS_DATABASE_URL raw values (dialect name only). |
| T-01-04 | Denial of service | harness/relay.py socket exposure | low | mitigate | Bind 127.0.0.1 with ephemeral ports only; relay lifetime scoped to the test. |
| T-01-05 | Repudiation | evidence bundle integrity | low | accept | Committed bundle is reviewed at the owner approval gate (D-11); CI artifacts carry the raw run output; git history provides change tracking. |
</threat_model>

<verification>
- `make verify` — complete profile for the locally selected dialect; single-command end-to-end run including host checkout verification, all p0 test modules present so far, and evidence emission (D-07, D-09).
- `make verify-sdk`, `make verify-host` — named subsets for the SDK security and host contract probes (D-06).
- `make verify-fast` — pins/provenance and quick local checks.
- On a PostgreSQL-available environment: `LNBITS_DATABASE_URL=<url> make verify` demonstrates the dialect switch through the same command surface (the 4-profile blocking matrix is owned by CI, D-03).
- CI: `.github/workflows/qualification.yml` runs the 4 blocking profiles and the macOS advisory profile; all blocking profiles must pass clean (D-10).
</verification>

<success_criteria>
- PINS.md fully records pins, platform matrix, artifact identities, provenance, install path, and pending owner approval (QUAL-01/P0-01).
- SDK security probes pass with the tested binary recorded (QUAL-02/P0-02).
- Host contract probes pass with lifecycle/failure semantics documented (QUAL-03/P0-03).
- One canonical command with named subsets works from repo root (D-06, D-07, D-12).
- Evidence bundle emitted per run (D-09); no raw logs committed.
- No production extension runtime code exists — only the qualification harness, CI definitions, PINS.md, evidence tooling, and fixtures.
</success_criteria>

<output>
Create `.planning/phases/01-conformance-profile-contract-phase-0/01-01-SUMMARY.md` when done
</output>
